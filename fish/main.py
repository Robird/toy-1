"""fish/main.py — 程序入口（M3-01 占位）。

正式装配（World + Renderer + InputSource + GameLoop）将在 M3-02 起逐步落地。
本步只保证 `python -m fish.main` 可无异常跑完。
"""

from __future__ import annotations


def main() -> None:
    """打印骨架就绪信息；骨架阶段不初始化 pygame。"""

    print("fish MVP — skeleton ready")


if __name__ == "__main__":
    main()
