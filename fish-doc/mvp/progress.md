# Fish MVP — 进度追踪（活文档）

> **每次开始/结束任务都必须更新本文。** 任何 subagent 接到 fish 项目相关任务时，先读 [00-overview.md](00-overview.md)，再读本文了解当前状态与未决问题。

---

## 总体里程碑

| 里程碑 | 状态 | 说明 |
|---|---|---|
| M0 设计文档（fish-doc/mvp/） | ✅ 完成 | 由主会话起草 |
| M1 引擎设计文档（toy-engine/mvp/） | ✅ 完成 | 由 Claude subagent 起草 |
| M2 toy-engine 实现 | ✅ 完成 | 372 passed，覆盖率 92%（rng/geom/recorder/metrics 100%）；详见 [toy-engine progress](../../toy-engine/mvp/progress.md) |
| M3 fish 业务实现 | ✅ 完成 | 10 步任务表见下；Claude 编码 + GPT 审阅 + 主会话 commit |
| M4 联调 + bot 跑分调参 | ⬜ 未开始 | |
| M5 MVP 验收 | ⬜ 未开始 | 按 [08 §6](08-mvp-scope.md) |

状态图例：✅ 完成 · 🟡 进行中 · 🔵 待审 · ⬜ 未开始 · ❌ 阻塞

---

## 任务表

| ID | 任务 | 状态 | 责任 | 关联文档 | 产出 / 验收结论 |
|---|---|---|---|---|---|
| M0-01 | overview + 术语表 | ✅ | 主会话 | [00](00-overview.md) | 已交付 |
| M0-02 | 核心循环 | ✅ | 主会话 | [01](01-core-loop.md) | 已交付 |
| M0-03 | 鱼群生态 | ✅ | 主会话 | [02](02-fish-ecosystem.md) | 已交付 |
| M0-04 | Boss 战 | ✅ | 主会话 | [03](03-boss.md) | 已交付 |
| M0-05 | 关卡生成器 | ✅ | 主会话 | [04](04-level-generator.md) | 已交付 |
| M0-06 | 视觉规范 | ✅ | 主会话 | [05](05-visuals.md) | 已交付 |
| M0-07 | 操作与手感 | ✅ | 主会话 | [06](06-controls-feel.md) | 已交付 |
| M0-08 | 测试脚手架 | ✅ | 主会话 | [07](07-test-harness.md) | 已交付 |
| M0-09 | MVP 验收标准 | ✅ | 主会话 | [08](08-mvp-scope.md) | 已交付 |
| M1-01 | toy-engine overview + 术语 | ✅ | Claude subagent | [`toy-engine/mvp/00-overview.md`](../../toy-engine/mvp/00-overview.md) | 字体选方案 A；音频暂不下沉 |
| M1-02 | RNG 模块设计 | ✅ | Claude subagent | [`01-rng.md`](../../toy-engine/mvp/01-rng.md) | 与 [07 §1](07-test-harness.md) 一致；`spawn` 用 BLAKE2b 派生 |
| M1-03 | Scene/World 抽象 | ✅ | Claude subagent | [`02-scene.md`](../../toy-engine/mvp/02-scene.md) | **Q6 决议**：不做 ECS，改极简 `GameLoop` |
| M1-04 | InputSource 抽象 | ✅ | Claude subagent | [`03-input.md`](../../toy-engine/mvp/03-input.md) | 引擎只提供 `BotInputBase`；具体 bot 启发式留 fish |
| M1-05 | Recorder 录像格式 | ✅ | Claude subagent | [`04-recorder.md`](../../toy-engine/mvp/04-recorder.md) | JSON +可选 gzip；含 config_hash |
| M1-06 | Metrics 收集器 | ✅ | Claude subagent | [`05-metrics.md`](../../toy-engine/mvp/05-metrics.md) | 引擎只提供框架，5 大指标语义留 fish |
| M1-07 | 几何工具 | ✅ | Claude subagent | [`06-geom.md`](../../toy-engine/mvp/06-geom.md) | 含 `Vec2 / circle / AABB / 角度`，无 numpy |
| M1-08 | 渲染封装 GeoCanvas | ✅ | Claude subagent | [`07-render.md`](../../toy-engine/mvp/07-render.md) | 含粒子/屏震/调色板；零内置颜色常量 |
| M1-09 | tools 脚本框架 | ✅ | Claude subagent | [`08-tools.md`](../../toy-engine/mvp/08-tools.md) | 业务通过 `GameFactory` 协议注入 |
| M1-10 | 引擎 MVP 验收标准 | ✅ | Claude subagent | [`09-mvp-scope.md`](../../toy-engine/mvp/09-mvp-scope.md) | |
| M1-11 | 引擎 progress | ✅ | Claude subagent | [`progress.md`](../../toy-engine/mvp/progress.md) | |

> M2 起的实现任务在 toy-engine 文档落地后再细化登记。

---

## M3 任务表（fish 业务实现）

采用 **Claude 编码 → GPT 审阅 + 顺手修复 → 主会话独立 commit** 的迭代流程，同 M2。每步开工前 subagent 必读 [00-overview.md](00-overview.md) + 本文 + 该步关联文档。每步完成后必须更新本文「工作日志」与本表「状态/提交」列。

模块布局参见 [00-overview.md §5](00-overview.md)。

| ID | 任务 | 状态 | 提交 | 关联设计文档 | 关键产出 |
|---|---|---|---|---|---|
| M3-01 | 项目骨架：包结构 + `fish/config/constants.py` + `fish/config/level_config.py`（dataclass）+ `fish/main.py` 占位 + 单测脚手架 | ✅ 已审，待提交 | — | [00](00-overview.md), [04](04-level-generator.md), [08](08-mvp-scope.md) | `fish/{__init__,main}.py`, `fish/config/*`, `tests/fish/test_skeleton.py`（5 passed；GPT review PASS_WITH_FIXES） |
| M3-02 | `World` 骨架：`Steppable` 协议（`step/snapshot/snapshot_hash/is_finished`）+ `GameResult` 枚举 + 实体基类；空逻辑但 `GameLoop` 能跑通 | ✅ 已审，待提交 | — | [00 §5](00-overview.md), engine [02](../../toy-engine/mvp/02-scene.md), 契约 #1/#2/#3 | `fish/world.py`（World + GameResult + snapshot_hash 规范化）, `fish/entities/base.py`（Entity 基类）, `fish/main.py`（30 帧 headless）, `tests/fish/test_world_skeleton.py`（GPT 补强后 tests/fish 25 passed；GPT review PASS_WITH_FIXES） |
| M3-03 | Player + 移动系统（惯性/turn_rate/边界反射）+ `KeyboardMouseInput` 接入（消费 `InputFrame.desired_dir: Vec2\|None`） | ✅ 已审，待提交 | — | [01](01-core-loop.md), [06](06-controls-feel.md), 契约 #2/#6 | `fish/entities/player.py`, `fish/systems/movement.py`, `fish/world.py` 接入 player + MovementSystem；GPT 审阅后按设计文档修正玩家初始 `tier=0`；snapshot 落实契约 #2；`fish/main.py` 以 `_StubInput` 驱动 30 帧证明实际移动（player_pos.x=724.92 > WORLD_W/2）；补强零向量防御、四向边界反射、30 帧平行决定性测试；`pytest tests/fish -q` 40 passed，全仓 418 passed 无回归 |
| M3-04 | Fish 实体 + `FishAI`(WANDER/FLEE/CHASE 三态) + 简化群行为 + `Spawner`(基础版,4 个非 Boss 等阶) | ✅ 已审，待提交 | — | [02](02-fish-ecosystem.md) | `fish/entities/fish.py`, `fish/ai/fish_ai.py`, `fish/systems/spawner.py`，`fish/world.py` 接入 spawner+fish_ai+`alloc_eid`，snapshot 增 fish 项；GPT 审阅补强 FLEE 零向量防护、separation 限速、spawn 瞬间屏外测试，并修正 `fish/main.py` 精确跑 300 帧；`tests/fish/test_fish_ai.py` 共 20 用例；`pytest tests/fish -q` **60 passed**，全仓 `pytest -q` **438 passed**（+20 vs M3-03 基线 418）；`python -m fish.main` 输出 `frames=300 elapsed_s=5.0000`、`fish_count_by_tier: tier1=8, tier2=0, tier3=0, tier4=0`（达成 WARMUP target=8）；GPT review PASS_WITH_FIXES |
| M3-05 | 碰撞检测 + 吃/被吃判定 + 成长（TIER 阈值）+ `GameResult.DEAD` + 同阶弹性反弹 | ✅ 已审，待提交 | — | [01](01-core-loop.md), [02 §3](02-fish-ecosystem.md) | `fish/systems/collision.py`（`CollisionSystem` + `can_eat` + `_elastic_bounce_same_tier`；GPT 审阅补强 DEAD 同帧短路与反弹非有限值防护）、`fish/systems/growth.py`（`GrowthSystem` 跨阈值升级）、`fish/entities/player.py`（+`exp` 字段 + `grow_to(new_tier)` 同步 radius/max_speed）、`fish/world.py`（step 顺序 spawner→fish_ai→movement→collision→growth→cleanup；hook `on_fish_eaten`/`on_player_eaten`/`on_player_grow`；snapshot 增 `player_tier`/`player_exp`/`stats`；DEAD 后仅推进计时器）、`fish/main.py`（600 帧 + `_ChaseNearestInput` 驱动 demo）。`tests/fish/test_collision.py` 新增 29 用例（Claude 26 + GPT 补强 3：DEAD 同帧短路、同心 bounce 非 NaN、一帧吃多鱼跨多阈值；另含 can_eat、1-tier 吃、严格吃、DEAD、同 tier bounce 位置/等质量法向互换、fish-fish bounce、tier 上限、决定性、DEAD 后不回退、snapshot 新字段等）；`pytest tests/fish -q` **89 passed**（+29 vs M3-04 基线 60），全仓 `pytest -q` **467 passed**（+29 vs M3-04 基线 438）无回归；`python -m fish.main` 输出 `frames=600 elapsed_s=10.0000 ... tier=0 exp=4.00 game_result=None stats={'fish_eaten_count': 2, ...} snapshot_hash=5fb8f3d0...`（seed=0 下追最近 fish 10s 吃掉 2 条 Tier-1，exp=4 < 8 未升级，未死亡）。未实现 LevelDirector/Boss/render/手感。新增「M3 实施期发现」 #13（can_eat 公式裁决）。GPT review PASS_WITH_FIXES；等主会话 commit。 |
| M3-06 | `LevelGenerator`（参数+校验+重试）+ `LevelDirector`（WARMUP/PRESSURE/BOSS/REVENGE 阶段切换）；接入 spawner | ✅ 已审，待提交 | — | [04](04-level-generator.md), [01 §4](01-core-loop.md) | `fish/systems/level_generator.py`（`LevelGenerator.generate(seed,difficulty,rng)->LevelConfig` + `validate(cfg)->[Violation]` + `LevelGenerationError`；用 `rng.spawn("level_gen").spawn(f"attempt_{k}")` 隔离重试子流，最多 `LEVEL_GEN_MAX_RETRIES=10` 次；难度 ∈ [0,1] 影响 PRESSURE Tier-4 上限与 boss.appear_time 偏置；5 大硬约束 C1=每阶段至少有可吃猎物（GPT 补强为按阶段可吃 tier 上限检查）/ C2=tier4 ≤ `PHASE_TIER4_POPULATION_MAX=3` / C3=WARMUP 仅 Tier-1..2 且 Tier-1 ≥ 1（裁决 #13 链）/ C4=boss.appear_time ∈ [25,60]s / C5=相邻阶段每档 max(a,b) ≤ 3·max(min(a,b),1) 防止难度跳变；BOSS/REVENGE PhaseConfig.duration_s 填新增的 `BOSS_PHASE_TIMEOUT_S=60.0` / `REVENGE_PHASE_TIMEOUT_S=30.0` 作事件驱动兜底；spawn_rate = `max(0.05, n/8.0)` 占位；GPT 审阅补强 NaN/inf/负数校验与错误上下文）；`fish/systems/level_director.py`（`LevelDirector` 维护 `current_phase / phase_elapsed_s / _boss_was_present / transition_log`；step 顺序在 World 中先于 spawner 跑：先判 `elapsed_s + dt ≥ TIMEOUT_S=180` → `GameResult.TIMEOUT`，再算迁移：WARMUP→PRESSURE 仅按 duration；PRESSURE→BOSS 按 (`PRESSURE.duration_s` 已到且 `elapsed_s + dt ≥ boss.appear_time_s`) ∨ `player.tier ≥ 2`；BOSS→REVENGE 按 `player.tier ≥ TIER_GIANT=4` ∨ (`boss_was_present ∧ boss is None`)；REVENGE 完成 → VICTORY；切换时把 `world._spawner._time_since_last_check=∞` 触发立即重新刷新；BOSS 阶段 `get_active_population_target()` 返回全 0 抑制普通鱼）；`fish/systems/spawner.py` 改读 `world.director.get_active_population_target()`；`fish/world.py` 在 `__init__` 中 spawner 之后构造 director，step 顺序 director→spawner→fish_ai→movement→collision→growth→cleanup，snapshot 增 `phase`/`phase_elapsed_s`，**`is_finished()` 仅依赖 `game_result is not None`（裁决 #4 已在本步落实，发现 #4 关闭）**；`fish/main.py` 重写为 `LevelGenerator.generate(seed=0, difficulty=0.5, rng=SeededRng(0))` + 60s headless + 改进版 `_ChaseNearestInput`（识别 prey/threat，威胁 < 200px 时径向逃离 + 0.6 朝中心偏置避免被推到墙角）。新增 `tests/fish/test_level_generator.py` 共 76 用例（Claude 68 + GPT 补强 8：非有限数值 / C1 可吃 tier 上限 / C5 上下文 / 重试递增 / boss.appear_time 下限 / REVENGE 恢复刷新 / snapshot phase hash）；同步修订 `tests/fish/test_world_skeleton.py::test_finished_when_total_duration_reached`→`test_not_finished_just_by_elapsed_s`、`tests/fish/test_world_skeleton.py::test_gameloop_stops_at_total_duration`→`test_gameloop_stops_when_game_result_set`，`tests/fish/test_fish_ai.py::test_eventually_reaches_warmup_target` 限制循环帧数到 8s 内（避免被 director 推进到 PRESSURE 改变 target）。`pytest tests/fish -q` **165 passed**（+76 vs M3-05 基线 89），全仓 `pytest -q` **543 passed**（+76 vs M3-05 基线 467）无回归；`python -m fish.main` 输出 `level: seed=0 difficulty=0.50 warmup=12.28s pressure=23.72s boss_appear=42.16s` 与 `phase_transitions (2): t=12.28s WARMUP→PRESSURE; t=42.17s PRESSURE→BOSS`，`frames=3601 elapsed_s=60.0167 player_pos=(886.60, 569.84) tier=2 exp=28.00 phase=BOSS phase_elapsed_s=17.85 game_result=None stats={'fish_eaten_count': 8, 'fish_eaten_tier1': 4, 'fish_eaten_tier2': 4, 'player_grow_count': 2} snapshot_hash=ec0c61032834c4d0777c5d60e89d961f5134153b`（DoD「至少 2 阶段切换」达成；60s 仍在 BOSS 且未实现 Boss，`game_result=None` 合理）。**未**实现 Boss 实体（M3-07）/ render（M3-08）/ 手感（M3-09）/ tools 联调（M3-10）。新增「M3 实施期发现」 #14（C5 阈值与 spawn_rate 公式来源）+ #15（裁决 #4 落实）。GPT review PASS_WITH_FIXES；等主会话 commit。 |
| M3-07 | Boss 实体 + `BossAI` 五核心状态 + ENRAGED 修饰位（PATROL/CHASE/CHARGE_WINDUP/CHARGE/STUNNED）+ 玩家复仇判定 + Tier4 提示 | ✅ 已审，待提交 | — | [03](03-boss.md) | ① `fish/entities/boss.py` 新增 `Boss(Entity)`（额外字段 `heading/hp/max_hp/state:object/state_timer/bite_count/intro_remaining/charge_cooldown_remaining/charge_dir:Vec2/enraged/sense_radius/chase_speed/turn_rate/charge_cooldown/rng:SeededRng`，`tier=BOSS_TIER=5` 作「超 tier」标记区分 Tier-4 Barracuda / Boss 体型）+ `Boss.spawn(eid,world_size,rng,*,player_pos=None,cfg_boss=None)`（按 player_pos 选离 player 最远那一边屏外 SPAWNER_EDGE_MARGIN 入场，朝中心，tiebreak 走 rng）。② `fish/ai/boss_ai.py` 新增 `BossState`（PATROL/CHASE/CHARGE_WINDUP/CHARGE/STUNNED 5 个核心态；`boss.enraged` 为修饰位）+ `BossAI.step(boss,world,dt)`：第一帧 None→PATROL；每帧检 `hp/max_hp < BOSS_ENRAGE_HP_RATIO=0.30` → `boss.enraged=True`（不改变 `BossState`）；intro_remaining 与 charge_cooldown_remaining 自减；强制 PATROL 8s 后按 `sense_radius` 入 CHASE；分发到 `_step_patrol/_step_chase/_step_charge_windup/_step_charge/_step_stunned`；CHARGE 撞 [0,W]×[0,H] → STUNNED + clamp + 启动冷却；ENRAGED 下 windup 缩短到 `BOSS_CHARGE_WINDUP_S * BOSS_ENRAGED_WINDUP_MUL=0.5` 倍、cooldown 缩短到 `BOSS_ENRAGED_COOLDOWN_MUL=0.6` 倍、sense 放大到 1.30 倍；boss 不参与 MovementSystem（`isinstance(ent, Boss): continue`），由 BossAI 自处理 + 非 CHARGE 时墙体反射。③ `fish/entities/player.py` 加 `invuln_remaining: float`（咬中 boss 后 `PLAYER_INVULN_AFTER_BITE_S=0.5s` 内不被任何鱼/Boss 致死，且节流连续重叠时的多次 boss bite）。④ `fish/systems/movement.py` 跳过 Boss。⑤ `fish/systems/collision.py` 升级：每帧自减 `player.invuln_remaining` + 计算 `world.tier4_warning = (player.tier<TIER_GIANT=4 ∧ ∃ alive 且屏内 fish.tier==4)`；fish↔player「fish 吃 player」分支在 invuln 期内跳过；新增 `_resolve_player_boss(world,player,boss)`：intro 期跳过；player.tier<4 且接触（且非 invuln）→ `on_player_eaten_by_boss(boss)`（写 DEAD + `death_cause_tier=boss.tier=5`）；player.tier==4 且 boss.STUNNED 任意角度 → `_bite_boss`；尾部弧（`angle_in_arc(rel_angle, boss.heading+π, math.radians(BOSS_TAIL_ARC_HALF_DEG=120))`，即尾后 240°）→ `_bite_boss`；其余正面/侧面接触（player.tier==4 非 invuln）→ DEAD；同心/非有限角度按危险接触处理；`_bite_boss`：`boss.hp -= BOSS_BITE_DAMAGE=1` + `bite_count++` + `state=STUNNED` + `player.invuln_remaining=0.5` + `world.on_boss_bitten`；hp<=0 → `boss.alive=False` + `world.on_boss_killed`（仅清 `world.boss=None` + 累计 stats，**不**写 game_result，留 director 在 REVENGE 段写 VICTORY）。⑥ `fish/world.py` 升级：`self.boss: Boss\|None=None` + `self.tier4_warning=False` + `self._boss_ai=BossAI()` + stats 加 `boss_bite_count/boss_killed`；step 顺序在 fish_ai 后跑 `_boss_ai.step` 再 movement；新 hooks `on_player_eaten_by_boss/on_boss_bitten/on_boss_killed`；`spawn_boss()` 幂等（用 `world.rng.spawn("boss")` + `Boss.spawn(... cfg_boss=cfg.boss)` + 加入 entities）；`_cleanup_dead` 同步清 dead boss；snapshot 增 `player_invuln_remaining/tier4_warning/boss`（`_boss_snapshot()` 含 heading/tier/hp/max_hp/state.name/enraged/intro_remaining/bite_count）。⑦ `fish/systems/level_director.py` 在 `_transition_to(Phase.BOSS)` 调 `world.spawn_boss()`，并把 `_boss_was_present` 标记下沉到 `world.boss is not None` 即置 True；Boss 死亡后先切 REVENGE，再等 REVENGE 窗口结束写 `GameResult.VICTORY`。⑧ `fish/main.py` 改 90s（5400 帧）+ `_ChaseNearestInput` 升级：boss 出现且 intro 完成 + player.tier>=4 时追 boss「尾点」`boss.pos - (cos,sin)(heading)*radius*1.2`；player.tier<4 时把 boss 当威胁；threat-flee 阈值 200→250px；改用 `loop.step_once(1)` 每帧抓 boss state 切换日志；打印 phase_transitions/boss_state_transitions/tier4_warning/boss summary。⑨ `fish/config/constants.py` 新增 `BOSS_TIER=5/BOSS_RADIUS=90.0/BOSS_BITE_DAMAGE=1/BOSS_PATROL_SPEED_RATIO=0.40/BOSS_PATROL_TURN_INTERVAL_S=2.5/BOSS_ENRAGED_WINDUP_MUL=0.50` 全部带「MVP 占位 + fish-doc/03 §6 来源」注释。⑩ `tests/fish/test_boss.py` 新增/补强 33 用例（含 ENRAGED 修饰位、无敌节流、同心角度防护、屏内 Tier-4 提示、HP=0→REVENGE→VICTORY 链路）。`pytest tests/fish -q` **198 passed**（+33 vs M3-06 基线 165）；全仓 `pytest -q` **576 passed**（+33 vs M3-06 基线 543）无回归；`python -m fish.main` 输出 `level: seed=0 difficulty=0.50 warmup=12.28s pressure=23.72s boss_appear=42.16s` + `phase_transitions (2): t=12.28s WARMUP→PRESSURE; t=42.17s PRESSURE→BOSS` + `boss_state_transitions (9): t=42.17s <none>→PATROL; t=50.57 PATROL→CHASE; t=51.20 CHASE→CHARGE_WINDUP; t=52.00 →CHARGE; t=53.52 →CHASE; t=62.98 →CHARGE_WINDUP; t=63.78 →CHARGE; t=64.93 →STUNNED; t=66.95 →CHASE`，`frames=4315 elapsed_s=71.9167 player_tier=1 game_result=DEAD tier4_warning=True boss: state=CHASE hp=3/3 bites=0 enraged=False`（player tier=1 在 BOSS 阶段被 boss 撞死，符合预期；boss 出现 + 9 次状态切换 + 完整 PATROL→CHASE→CHARGE_WINDUP→CHARGE→STUNNED 闭环已验证）。**未**实现 render（M3-08）/ 手感粒子（M3-09）/ bot/metrics/tools（M3-10）。新增「M3 实施期发现」 #16（Boss tier 编号取 5 vs 共享 4）+ #17（boss 自管移动 vs MovementSystem 统一）+ #18（尾部弧与正面弧二分法 vs fish-doc 二分法；未发现侧面中性带）。GPT review PASS_WITH_FIXES；等主会话 commit。 |
| M3-08 | 视觉：`Palette` 配色 + `draw_fish` / `draw_boss` + 视差背景 + UI + 字体（`toy_engine.font.load_font`） | ✅ 已审，待提交 | — | [05](05-visuals.md), engine [07](../../toy-engine/mvp/07-render.md) | ① `fish/render/palette.py`：`FISH_PALETTE`（基于 `toy_engine.render.Palette`）锁定 fish-doc/05 §1 五个背景色 + 五个角色色 + UI/Boss 状态附加色，`tier_to_role_name(fish_tier, player_tier)` 经 GPT 审阅修正为「同 tier→peer；可吃一档→prey；其余→threat」，并验证 player.tier=4 时普通鱼 threat 退化为 prey/peer。② `fish/render/visuals.py`：纯函数式绘制（不直接 import pygame，全部走 `GeoCanvas`）：`draw_player`/`draw_fish`/`draw_boss`（含 STUNNED 旋转星 / ENRAGED 红边 / CHARGE_WINDUP 红线 / 嘴 1.5s 张合 / 双眼按状态变色）、`draw_parallax_background`（far 径向 + 5 丛贝塞尔海草 + 30 气泡，按 player_offset×{0.1,0.4,1.0} 做视差且 clamp 到 30px；GPT 审阅修复 far 层未实际使用 0.1× 偏移的问题）、`draw_ui`（左上 Tier 徽章 + exp 进度条；右上 elapsed/phase；顶部 Tier-4 警示；右下 Boss HP + ENRAGED 标）、`draw_game_over`（DEAD/VICTORY/TIMEOUT 三色大字 + 半透遮罩）。③ `fish/render/pyg_renderer.py`：`PygRenderer(canvas, palette, font)` 纯函数式 `render(world)` 顺序 background→fishes(按 eid)→player→boss→ui→game_over。④ `fish/main.py` 升级：保留 `run_headless`（M3-04..07 兼容入口）+ 新增 `run_gui(seed,difficulty,title,end_screen_seconds=3.0)`（开 1280×720 窗口，KeyboardMouseInput viewport=(W,H)，自管 ESC/QUIT 事件循环 + `loop.step_once(1)` 单步 + 终态保留 3s）；`main()` 默认走 GUI，`python -m fish.main --headless` 走旁路。⑤ 字体走 `toy_engine.font.load_font(20, "consolas", "microsoftyahei", fallback_size=20)`。⑥ `tests/fish/test_render.py` 共 29 用例（Palette 4 + Background 3 + DrawEntities 7 + UI 6 + PygRenderer 6 + ModuleImports 2；GPT 审阅在既有用例内补强 tier→role 与 far 视差 clamp 断言；要点：边界 `boss=None`/`player=None`/`fish=None` 不崩；boss 各状态 ×{enraged True/False} 笛卡尔积绘制不崩；UI font=None 时 silently 跳过；PygRenderer 跑 60 帧 stepped world 不崩；终态遮罩三态都画；visuals/pyg_renderer 模块顶层不直接 import pygame）。⑦ 同步修订 `tests/fish/test_skeleton.py::test_main_runs` 改 monkeypatch `sys.argv=["fish.main", "--headless"]`（main 默认入口已是 GUI）。GPT 审阅后 `pytest tests/fish -q` **227 passed**；全仓 `pytest -q` **605 passed**；`python -m fish.main --headless` 保持旁路输出，最终 `frames=4315 elapsed_s=71.9167 game_result=DEAD snapshot_hash=3b5b5beb82db79074906767d87adb60a6fb28818`。**未**实现 手感粒子（M3-09）/ bot/metrics/tools 联调（M3-10）。新增「M3 实施期发现」 #19（gradient_ellipse 替代 fish-doc/05 §3 「dark→light 纵向渐变」中"按行渐变 + 椭圆 mask"约定 vs 引擎 `gradient_ellipse(mode="linear")` 的语义对齐）+ #20（fish-doc/05 §4 boss 「3~4 段贝塞尔轮廓」MVP 用 `gradient_ellipse` 简化 vs 真贝塞尔轮廓）+ #21（fish-doc/05 §5 caustics noise 层 MVP 暂用径向渐变占位，未做 perlin）。GPT review PASS_WITH_FIXES；等主会话 commit。 |
| M3-09 | 手感：拖尾 + squash + 吃鱼三件套粒子 + 屏震 + 慢镜 + 渐隐；集成进 World/render | ✅ 已审，待提交 | — | [06](06-controls-feel.md) | `fish/render/feel.py`（TrailRenderer 12 点 / SquashRenderer 高速反向单调 / FeelEventBus 队列 / FeelEffects 组合 ParticleSystem+ScreenShake+slow-mo+flash+飘字）, `fish/world.py` listener registry + `_emit`, `fish/render/visuals.py::draw_player(squash=...)`, `fish/render/pyg_renderer.py::render(world, *, feel=None)`, `fish/main.py::run_gui` 接 feel, `tests/fish/test_feel.py` 17 用例（含 hash 序列与 listener 无关的关键不变量；GPT 审阅补强 listener warning、非有限 squash、GameLoop callable dt_scale 断言）；GPT 审阅顺手修复 `_emit` 静默吞异常→`warnings.warn`、`SquashRenderer`/visuals 非有限值防护、`boss_bitten` 屏震对齐 fish-doc/06 §3 的 6px×0.4s；`pytest tests/fish -q` **244 passed**（+17 vs M3-08 基线 227），全仓 **622 passed** 无回归；`python -m fish.main --headless` 仍输出 `snapshot_hash=3b5b5beb82db79074906767d87adb60a6fb28818`（与 M3-08 严格一致，决定性未变）。新增「M3 实施期发现」 #22（fish-doc/06 §3 慢镜 0.3s vs 任务书 1s 差异）。GPT review PASS_WITH_FIXES；等主会话 commit。 |
| M3-10 | `BotInput`（继承 `BotInputBase`）+ `MetricsCollector` 绑定（`bind_metrics` hook，闭合 EQ12）+ `GameFactory` 实现 + `tools/run_headless` `tools/param_sweep` 联调跑通 | ✅ 已审，待提交 | — | [07](07-test-harness.md), engine [08](../../toy-engine/mvp/08-tools.md), 契约 #4/#8 | `fish/ai/bot_player.py`（BotInput 继承 BotInputBase；启发式 5 优先级：威胁逃避→反杀 Boss/远离 Boss→追猎物→漂移；snapshot dict 驱动；零向量/非有限值随机抖动走 self.rng）, `fish/io/metrics_adapter.py`（FishMetricsListener：events→record_event；on_frame_end 维护 starvation_dt/near_miss rising-edge/boss_appear_t/player_max_tier；`write_envelope_before_finish()` 在 fallback 路径前写 5 大原料；`_finalize` 在 game.is_finished 时主动 finish）, `fish/factory.py`（FishGameFactory 实现 toy_engine.tools_lib.GameFactory 协议 5 方法 `make_level_config / make_world / make_bot / serialize_config / deserialize_config`；**注意签名以 toy_engine 代码为准，与任务书原文 `create_world / create_input / is_finished / get_result` 不一致，发现 #23**；`bind_metrics` 闭合 EQ12：注册 listener + wrap `world.step` 接管 tick + wrap `metrics.finish` 兜底写 5 大指标；模块级 `make_factory = FishGameFactory()` singleton 供 `--factory fish:make_factory` 加载；`FISH_FACTORY` 别名兼容 `DEFAULT_FACTORY_SPEC`）, `fish/__init__.py` re-export, `fish/__main__.py` 兼容 `fish.__main__:FISH_FACTORY` 默认 spec, `fish/main.py` 新增 `--bot` 旁路 GUI 用 BotInput, `tests/fish/test_bot.py`（11 用例：BotInputBase 子类性 + 漂移 + 威胁逃避 + 追猎物 + tier+1 视为 prey + 远离 boss + STUNNED 反杀 + 同 seed 决定性 + 30s 跑不抛）, `tests/fish/test_metrics_adapter.py`（11 用例：6 顶层字段全有 + 5 metrics 段 + engine_extras + result ∈ {DEAD/VICTORY/TIMEOUT} + fail_rate=null + JSON 可序列化 + starvation_ratio ∈ [0,1] + near_miss_count 非负 int + duration_s 一致 + duration_frames > 0 证明 EQ12 接管 tick）, `tests/fish/test_tools_integration.py`（2 子进程用例：`python -m tools.run_headless --factory fish:make_factory --seed 0 --out tmp.json` + `python -m tools.param_sweep --factory fish:make_factory --difficulty 0.5 --seeds 5 --out tmp.csv`）。`pytest tests/fish -q` **266 passed**（+22 vs M3-09 基线 244）；全仓 `pytest -q` **644 passed**（+22 vs M3-09 基线 622）无回归。Tools 联调（180s 默认 max-sim）：`run_headless` envelope 顶层 6 字段 + metrics 5 项全有，`param_sweep` 输出 1 行（1 difficulty × 5 seeds）。**EQ12 状态：✅ 闭合 —— bind_metrics hook 通过 wrap step + finish 双道接管 metrics tick / 终态写入，`duration_frames > 0` 与 `metrics.<5项>` 全部出现验证 hook 实际生效**。新增「M3 实施期发现」 #23（GameFactory protocol 任务书 vs 代码签名差异）。等主会话 commit + GPT review。 |

> M3 全部完成后进入 M4：用 `tools/param_sweep.py` 跑 100 局 headless 验证 5 个 metric 落入 [08 §3](08-mvp-scope.md) 的目标区间，必要时回头微调参数。

---

## 未决问题（Open Questions）

| # | 问题 | 来源文档 | 状态 |
|---|---|---|---|
| Q1 | 是否引入"饥饿衰减"？ | [01 未决](01-core-loop.md) | 暂否决（MVP 不做） |
| Q2 | 是否引入"营养鱼" | [02 未决](02-fish-ecosystem.md) | 暂否决 |
| Q3 | 玩家咬 Boss 是否要付出"缩水"代价 | [03 未决](03-boss.md) | 暂否决 |
| Q4 | LevelGenerator 是否引入洋流方向场 | [04 未决](04-level-generator.md) | 暂否决，留接口 |
| Q5 | Bot 多档聪明度（low/mid/stress） | [07 未决](07-test-harness.md) | MVP 只做一档 |
| Q6 | toy-engine 是否提供 Scene/System 抽象，还是只做底层工具集？ | M1 起草时决定 | ✅ **已决议（2026-04-27, Claude subagent）：不提供 ECS / Scene / System，改提供可选的极简 `GameLoop` helper（固定步长 + 输入/逻辑/渲染解耦）。** fish 的 `World` 直接以 `Steppable` 协议（`step / snapshot / is_finished`）接入。理由与反例条件见 [`toy-engine/mvp/02-scene.md §1`](../../toy-engine/mvp/02-scene.md)。|

---

## 与 toy-engine 的接口假设清单

> fish-doc 在写作时**假设**以下 import 可用。toy-engine subagent 在 M1 起草时如需变更命名或参数，**必须**在本表追加并通知主会话裁决。

```python
from toy_engine.rng     import SeededRng
from toy_engine.input   import InputSource, InputFrame, KeyboardMouseInput, ReplayInput
from toy_engine.recorder import Recorder
from toy_engine.metrics import MetricsCollector
from toy_engine.geom    import Vec2, circle_circle_overlap, clamp, lerp
from toy_engine.render.pyg import GeoCanvas   # 几何绘制高阶封装（可选）
# Scene/System 抽象是否提供 → Q6 已决议：不提供，改用 GameLoop（见下方变更登记 #1）
```

变更登记区（subagent 修改时追加，不要删除历史）：

| # | 时间 | 变更 | 提出方 | 主会话裁决 |
|---|---|---|---|---|
| 0 | — | （初始） | — | — |
| 1 | 2026-04-27 | **`Scene / System` 不下沉**。改提供可选的 `from toy_engine.loop import GameLoop`。fish 的 `World` 不必继承任何引擎基类，仅按 `Steppable` 协议实现 `step / snapshot / is_finished` 三个方法。详见 [`toy-engine/mvp/02-scene.md`](../../toy-engine/mvp/02-scene.md)。 | Claude subagent | ✅ 通过（主会话, 2026-04-27）：YAGNI 优先，避免为 N=1 样本过度抽象。fish 实现 subagent 须按此约定。 |
| 2 | 2026-04-27 | **新增隐含契约**：`world.snapshot()` 需暴露 `player_pos: tuple[float, float]` 字段，供 `KeyboardMouseInput.poll` 计算鼠标→玩家方向。详见 [`toy-engine/mvp/03-input.md §4.1`](../../toy-engine/mvp/03-input.md)。 | Claude subagent | ✅ 通过：fish 实现 subagent 必须在 `World.snapshot()` 中包含 `player_pos`。 |
| 3 | 2026-04-27 | **新增隐含契约**：`World.snapshot_hash() -> str` 由业务实现，供 `tools/run_headless.py --determinism-check` 比对帧序列。详见 [`toy-engine/mvp/08-tools.md §5`](../../toy-engine/mvp/08-tools.md)。 | Claude subagent | ✅ 通过：fish 实现时把所有可观察状态做稳定哈希（建议 sorted-key JSON + sha1）。 |
| 4 | 2026-04-27 | **`BotInput` 不在引擎下沉**。引擎只提供 `BotInputBase`（接 `SeededRng` 的构造基类），具体启发式留 `fish/ai/bot_player.py`（启发式属业务知识）。fish-doc 07 §5 的 BotInput 不再 import 自 `toy_engine.input`。 | Claude subagent | ✅ 通过：启发式确属业务。M3 实现 subagent 在 fish-doc/mvp/07 §5 顶部补一句"实现位于 fish/ai/bot_player.py，继承 toy_engine.input.BotInputBase"。 |
| 5 | 2026-04-27 | **新增引擎导出**（fish-doc 假设之外但合理的扩展，不破坏既有 import）：`InputFrame`, `BotInputBase`, `Recording`, `GameLoop`, `Palette`, `ParticleSystem`, `ScreenShake`, `AABB`, `aabb_overlap`, `wrap_angle`, `angle_lerp`, `smoothstep`, `lerp_vec`, `circle_circle_penetration`, `toy_engine.font.load_font`, `toy_engine.tools_lib.GameFactory`。 | Claude subagent | ✅ 通过：纯增量扩展，不破坏现有契约。**注**：原列的 `RunInfo` 已在契约 #8 metrics 重写中废弃。 |
| 6 | 2026-04-27 | **澄清/重申**：`InputFrame.desired_dir` canonical 类型为 `Vec2 | None`（fish-doc/mvp/06 §4 为权威）。引擎 03-input、04-recorder 已对齐。`Vec2` 接受 `Vec2Like` 入参便于传 tuple；录像 JSON wire format 为 `[x, y]` 数组。 | 主会话（R2 修订） | ✅ 通过：是澄清非变更。 |
| 7 | 2026-04-27 | **新增 geom 导出**：`Vec2Like`（输入类型联合）、`angle_delta(a, b)`、`rotate_toward(current, target, max_step)`、`angle_in_arc(angle, center, half_width)`。这些是 fish 的 Boss "尾部 240° 判定"与鱼群 turn_rate 转向所必需的便捷工具，不是新机制。详见 [`toy-engine/mvp/06-geom.md`](../../toy-engine/mvp/06-geom.md)。 | GPT subagent (R2) | ✅ 通过：纯增量便捷函数，不破坏现有契约。 |
| 8 | 2026-04-27 | **MetricsCollector envelope 与 API 改名**：单局 JSON 严格遵循 fish-doc/07 §6 envelope（顶层 `seed/difficulty/result/duration_s/player_max_tier/death_cause` + `metrics` 段 + 引擎附加 `engine_version/duration_frames/events/extra`）。新核心 API：`set_scalar(name, value, *, top_level=False)`、`record_event(name, value=None)`、`final_report() -> dict`、`finish(result, **extra)`。旧名 `event`/`to_dict` 作为兼容别名保留至 M3。详见 [`toy-engine/mvp/05-metrics.md`](../../toy-engine/mvp/05-metrics.md)。 | 主会话裁决 + Claude subagent 实施 (R2) | ✅ 通过：与 fish-doc/07 §6 完全对齐，保持 Q6 推/拉模型不变。 |

---

## M3 实施期发现

> 由 M3 阶段 subagent 在实施过程中登记的、与设计文档不一致或文档存在歧义/缺失之处。**禁止 subagent 自行修改设计文档**，由主会话裁决后再回填。

| # | 时间 | 步骤 | 发现 | 提出方 | 主会话裁决 |
|---|---|---|---|---|---|
| 1 | 2026-04-28 | M3-01 | fish-doc/04 §2 的 `LevelConfig.phases: dict[Phase, PhaseConfig]` 引用了 `Phase` 类型但全套 fish-doc 未给出其形式定义（00 §4.4 只以 `PHASE_WARMUP / PHASE_PRESSURE / PHASE_BOSS / PHASE_REVENGE` 列出四个阶段名）。M3-01 实现选择把 `Phase` 定义为 `enum.Enum`，成员名为 `WARMUP/PRESSURE/BOSS/REVENGE`（去掉 `PHASE_` 前缀以避免与枚举类名叠词），放在 `fish/config/constants.py`。 | Claude subagent (M3-01) | ✅ 通过（主会话, 2026-04-28）：成员名 `WARMUP/PRESSURE/BOSS/REVENGE` 是约定俗成的 enum 风格。M3-06 实现 LevelGenerator 时若涉及录像/JSON 序列化，使用 `Phase.<name>.name` 字符串落盘以保持稳定。fish-doc/00 §4.4 暂不回填（属业务约定，作为 M3 实现层细节合理）。 |
| 2 | 2026-04-28 | M3-01 | fish-doc/04 §2 的 `PhaseConfig` / `BossConfig` / `LevelConfig` 三者均写作 `@dataclass`（默认非 frozen），但 M3-01 任务书要求 `LevelConfig` 必须 `frozen=True`。当前实现：仅 `LevelConfig` 顶层 frozen，`PhaseConfig`/`BossConfig` 与文档一致保持非 frozen；后果是嵌套 `cfg.phases[Phase.WARMUP].duration_s = X` 仍可被改写。 | Claude subagent (M3-01) | ✅ 通过（主会话, 2026-04-28）：M3-01 不强制深度不可变。MVP 阶段 LevelGenerator 产出后约定为「只读使用」，靠纪律而非语言机制。如 M3-06 实现期发现误改导致 bug，再升级为全链 frozen。 |
| 3 | 2026-04-28 | M3-02 | fish-doc/01 §4 的 `GameResult` 列出 4 个枚举值（`RUNNING / DEAD / VICTORY / TIMEOUT`），但 M3-02 任务书举例时写「至少含 `WIN`、`DEAD`」。当前实现按 fish-doc/01 §4 取 4 态枚举（`RUNNING/DEAD/VICTORY/TIMEOUT`），且 `World.snapshot()['game_result']` 在「进行中」用 `None` 表达（不写 `RUNNING.name`），便于 JSON 友好与「终态写入即 finished」的判定一致。`RUNNING` 成员保留以兼容文档枚举完备性，但 M3-02 路径不会写入。 | Claude subagent (M3-02) | ⬜ 待裁决：建议 ① 接受 `None` ↔「进行中」的 snapshot 表达；② 保留 `RUNNING` 成员仅作枚举完备性；③ 后续 M3-05/07 写终态时必为 `DEAD/VICTORY/TIMEOUT` 之一。 |
| 4 | 2026-04-28 | M3-02 | fish-doc 未明示 `LevelConfig.total_duration` 计算公式。fish-doc/04 §2 中 `PhaseConfig.duration_s` 对 BOSS/REVENGE 两阶段为「事件驱动」(M3-01 默认实现填 `0.0`)；fish-doc/01 §4 给出独立的硬上限 `TIMEOUT_S=180`。当前 `World.is_finished()` 暂取 `Σ phase.duration_s`（默认 = WARMUP 15 + PRESSURE 20 + 0 + 0 = 35s），并在 docstring 注明「正式终态判定由 M3-05/07 接入 LevelDirector」。 | Claude subagent (M3-02) | ⬜ 待裁决：建议明确 ① `is_finished()` 的 fallback 是否应改为 `min(Σ phase.duration_s, TIMEOUT_S)`；② BOSS/REVENGE 阶段的 `duration_s` 字段语义（是「上限」还是「事件驱动占位 0」），并在 fish-doc/04 §2 回填。 |
| 5 | 2026-04-28 | M3-02 | fish-doc/00 §5 + 01 §1 未对 `Entity` 基类字段集合给出权威清单。当前实现取最小公共子集（`eid: int / pos: Vec2 / vel: Vec2 / radius: float / alive: bool`），把 `tier / hp / state / ai_state` 等留给具体子类按需追加。`eid` 字段是 fish 业务侧约定（`snapshot` 排序与 `snapshot_hash` 的稳定性所需），fish-doc 未明示但与 toy-engine/08 §5 「按 id 排序」隐含一致。 | Claude subagent (M3-02) | ⬜ 待裁决：建议在 fish-doc/00 §5 或 01 §1 回填 `Entity` 字段权威清单（含 `eid` 的来源说明），便于 M3-04/07 子类对齐。 |
| 6 | 2026-04-28 | M3-03 | M3-03 任务书明说 `Player.tier` 初始=1，但 fish-doc/00 §4.1 + 01 §2 「玩家初始 = Tier 0 fry」。Claude 初版按 tier=1；GPT 审阅后已修正当前实现为 `TIER_FRY`（tier=0，对应 `PLAYER_RADIUS[0]=10`、`PLAYER_MAX_SPEED[0]=220`），与设计文档和 `TIER_THRESHOLDS[0]=0` 对齐。 | Claude subagent (M3-03) + GPT review | ⬜ 待裁决：建议接受当前实现（tier=0）并视为任务书口误；若主会话确认 tier=1，需反向改代码并同步 fish-doc/00 §4.1、01 §2 与成长阈值语义。 |
| 7 | 2026-04-28 | M3-03 | fish-doc/06 §2 只给出单一标量的 `PLAYER_TURN_RATE=6.0` / `PLAYER_ACCEL=900` / `PLAYER_DRAG=3.5`（非 tier→table）；但 M3-03 任务书说「按 06 的 tier→数值表初始化」。当前实现：仅 `max_speed` 按 tier 查 `PLAYER_MAX_SPEED[tier]`；`accel` / `turn_rate_rad_s` 为全 tier 共享的文档标量。`PLAYER_DRAG` 作为 `desired_dir is None` 时的衰减依据（`vel *= exp(-drag*dt)`）。 | Claude subagent (M3-03) | ⬜ 待裁决：建议在 fish-doc/06 §2 明确 “MVP 阶段 turn_rate / accel / drag 为 tier 不分型”，或者在 M4 调参期补充 tier→table。 |
| 8 | 2026-04-28 | M3-03 | fish-doc/06 §2 仅提供玩家闪避 / 惯性参数，未说明「边界反射是否取玩家半径」。当前实现仍按 `[0, WORLD_W] x [0, WORLD_H]` 钉点反射（不减去 radius），与 M3-03 任务书「边界为 [0, WORLD_W]」一致；GPT 审阅补强了精确贴边且速度向外时立即反射的防粘墙保护。后续如需「鱼可见部分不出屏」，可在 M3-09 手感调优时改为留 radius 边距。 | Claude subagent (M3-03) + GPT review | ⬜ 待裁决（低优）：建议 MVP 暂保持点边界，半径边距留给 M3-09/M4 手感调参。 |
| 9 | 2026-04-28 | M3-04 | **任务书 vs 设计文档 tier 编号不一致**。M3-04 任务书写「fish.tier 范围 0..3（不含 Boss）」，但 fish-doc/02 §1 明确给出 4 类普通鱼为 **Tier 1..4**（Minnow=1 / Sardine=2 / Snapper=3 / Barracuda=4）；fish-doc/00 §4.1 同时写「Tier 0=fry 玩家初始」「Tier 4=giant Boss 体型档位」。MVP 实现选择以 fish-doc/02 §1（本步权威）为准：`Fish.tier ∈ {1,2,3,4}`，Tier 0 仅玩家初始，Boss 是独立实体（M3-07 接入），与 Barracuda 共享 Tier 4 的「体型档位」语义但不共享类。LevelConfig.default() 的 population_target 字典键 1..4 直接对齐。 | Claude subagent (M3-04) | ⬜ 待裁决：建议 ① 接受 fish.tier ∈ {1,2,3,4} 的实现；② 在 fish-doc/00 §4.1 注释一句「Tier 4 既是 Barracuda 的体型档位也是 Boss 的体型档位，两者按实体类型区分而非 tier」；③ 后续任务书统一引用「02 §1 的 1..4」。 |
| 10 | 2026-04-28 | M3-04 | fish-doc/02 §1~§4 给出了 FishParams 字段名（perception_radius / flee_radius / chase_radius / turn_rate / school_weight）但**未列具体数值**；fish-doc/02 §2 也未给出 WANDER 状态的 turn 间隔 / 巡航速度比例。MVP 实现自填 `FISH_TURN_RATE_RAD_S = (0, 5, 4, 3, 2.5)`、`FISH_PERCEPTION_RADIUS = FISH_FLEE_RADIUS = FISH_CHASE_RADIUS = (0, 100, 140, 180, 240)`、`WANDER_TURN_INTERVAL_S=1.0`、`WANDER_SPEED_RATIO=0.4`、`WANDER_HEADING_JITTER_RAD=1.0`、`FISH_SEPARATION_OVERLAP_MUL=1.5`、`FISH_SEPARATION_PUSH_SPEED=30.0`，全部在 `fish/config/constants.py` 加「MVP 占位 / M4 调参」来源注释。 | Claude subagent (M3-04) | ⬜ 待裁决：建议接受占位值进入 M4 bot 跑分，再根据 metrics 调整；若主会话希望此刻就在 fish-doc/02 §2 回填这些表，则需主会话给出权威值。 |
| 11 | 2026-04-28 | M3-04 | fish-doc/02 §2 的 FSM 含 `aggression` 标志与 `flee_radius * 1.5` 的滞回阈值；M3-04 任务书允许 MVP 简化为「player.tier > fish.tier → FLEE；player.tier < fish.tier → CHASE；相等 → WANDER」（无滞回 / 无 aggression）。当前实现采纳任务书简化版 + 距离门（`flee_radius` / `chase_radius` 内才切），其它行为差异通过 tier 数值与 perception_radius 隐式表达。完整 §2 滞回 / aggression 留 M4 调参。 | Claude subagent (M3-04) | ⬜ 待裁决：建议接受 MVP 简化 FSM；若 bot 跑分阶段出现「FLEE/WANDER 帧间反复抖动」（02 DoD 第 2 条），再补滞回阈值。 |
| 12 | 2026-04-28 | M3-04 | fish-doc/02 §4 末段写明硬约束「屏内 `count(tier <= player.tier) >= 1` 否则强制就近 spawn 一条 `tier == player.tier` 的猎物」与「Tier-4 在 PHASE_WARMUP 绝不出现」。M3-04 Spawner **未**实现这两条兜底：第 1 条属业务/director 层，留 M3-06 LevelDirector 接入；第 2 条目前依赖 `LevelConfig.default()` 中 WARMUP 的 `population_target[3]=population_target[4]=0` 自然满足，并未写显式 assert。 | Claude subagent (M3-04) | ⬜ 待裁决：建议两条兜底都留 M3-06；M3-06 实施期在 LevelDirector / Spawner 边界明确各自责任。 |
| 13 | 2026-04-28 | M3-05 | **Tier-0 玩家开局可吃 Tier-1 鱼**。fish-doc/01 §3 写「self > other 可吃」但 player.tier=0 + 最小 fish.tier=1 会卡死。M3-05 按主会话裁决实现 `can_eat(eater, victim) = eater.tier >= victim.tier - 1`（「以小搏大一档」），并在 `CollisionSystem.step` 中**优先**走「same-tier → bounce」分支以保留 fish-doc/01 §3 「同阶弹开」语义（任务书原文 elif 顺序若严格执行会因 `can_eat` 对同 tier 也返回 True 而把 bounce 永久吞掉）。公式同时适用于 player↔fish；NPC 之间不互吃为另一 MVP 裁决（避免群体灭绝），仅同 tier bounce。 | 主会话裁决 + Claude subagent (M3-05) | ✅ 通过（主会话, 2026-04-28）：M3-05 已实现。M3-06 LevelDirector / M3-07 Boss 判定须守住同一套语义；fish-doc/01 §3 会在 M3 收尾后补一句「玩家以小搏大一档 + 同 tier 优先 bounce」裁决注。 |
| 14 | 2026-04-28 | M3-06 | **LevelGenerator 抽样区间 + 校验阈值 + spawn_rate 公式均为 MVP 占位**。fish-doc/04 §3 给出每阶段 population 的「方向性」描述（WARMUP=「Tier 1 占多」、PRESSURE=「补 Tier 2/3，可少量 Tier 4」等）但**未给具体数字区间**；§4 列出 5 大硬约束的「方向」（C1/C2 引用了具名常量；C3 写「WARMUP 不出现 Tier 3+」未提 Tier 1 ≥ 1；C4 写「Boss 出现时间 25-60s」；C5 写「相邻阶段每档不得跳变 3x 以上」）但**未给「跳变 3x」的精确公式**。M3-06 的实现选择：① WARMUP `_WARMUP_TIER1_RANGE=(6,10)`、PRESSURE `(T1,T2,T3,T4)=((5,8),(2,3),(1,3),(0,2))`、BOSS `((4,7),(2,4),(1,3),(0,2))`、REVENGE `((3,5),(2,3),(0,2),0)` 全部带 「MVP 占位 / M4 调参」注释；② 难度 ∈ [0,1] 仅微调 PRESSURE Tier-4 上限 + boss.appear_time 偏置（高难提早），其它档位不受影响；③ C3 的「Tier 1 ≥ 1」额外加紧（裁决 #13 链上玩家 tier=0 必须有可吃猎物）；④ C5 公式精确化为「相邻阶段同档 max(a,b) ≤ 3·max(min(a,b), 1)」（min 加 1 避免 0→N 全部触发；min(a,b)=0 时 max(a,b) 上限 = 3）；⑤ spawn_rate = `max(0.05, n/8.0)`（n=该 tier 的 population_target）作为「每秒检查时刷新概率」占位；⑥ BOSS/REVENGE 的 `PhaseConfig.duration_s` 填 `BOSS_PHASE_TIMEOUT_S=60.0` / `REVENGE_PHASE_TIMEOUT_S=30.0` 作事件驱动**兜底**（实际应靠 player.tier 升级 / boss 死亡触发，duration 仅在卡关时兜底）。 | Claude subagent (M3-06) | ⬜ 待裁决：建议 ① 接受占位区间 + spawn_rate 公式进入 M4 bot 跑分，再按 metrics 调参；② 在 fish-doc/04 §4 回填 C3 的「Tier 1 ≥ 1」与 C5 的 `max(a,b) ≤ 3·max(min(a,b),1)` 精确公式；③ 在 fish-doc/04 §2 回填 BOSS/REVENGE 的 `duration_s` 语义（事件驱动的兜底上限，不是「正常持续时间」）。 |
| 15 | 2026-04-28 | M3-06 | **裁决 #4 已在本步落实**。M3-02 实施期发现 #4 提出 `World.is_finished()` 用 `Σ phase.duration_s` 占位的临时方案，本步按主会话裁决（建议方向「事件驱动 + TIMEOUT_S 兜底」）实施：`is_finished()` 简化为 `return self.game_result is not None`，所有终态由 LevelDirector 统一写入（DEAD 由 CollisionSystem 写、TIMEOUT 由 director 在 `elapsed_s + dt ≥ TIMEOUT_S=180` 写、VICTORY 由 director 在 BOSS 死亡 + REVENGE 完成或 REVENGE 阶段超时写）。`_total_duration_s` 字段在 World 中保留但不再被读取，仅留向后兼容标记。`tests/fish/test_world_skeleton.py` 中两条「按 elapsed_s 终止」的旧测试已改写为「不按 elapsed_s 终止」与「按 game_result 终止」。发现 #4 关闭。 | Claude subagent (M3-06) | ✅ 通过（主会话, 2026-04-28，按 #4 建议方向落地）。 |
| 16 | 2026-04-28 | M3-07 | **Boss tier 数值取 5（"超 tier"）而非共享 Tier-4**。fish-doc/00 §4.1 注明「Tier-4 既是 Barracuda 体型也是 Boss 体型」（裁决 #9 的延伸）但**实体类型不同**。M3-07 实现层选择给 `Boss.tier = BOSS_TIER = 5`（新增常量，独立于 fish 的 `tier ∈ {1..4}` 与 `TIER_GIANT=4`）。理由：① snapshot 中可单独区分 Boss / Barracuda；② `can_eat(player, boss)` 公式 `player.tier >= boss.tier-1 = 4` 自然让 Tier-4 玩家不会"误吃"Boss；③ Boss 与 player 的判定走自己的 `_resolve_player_boss`，不复用 `can_eat`。`death_cause_tier` 写 `boss.tier=5`，便于跑分时区分死因。 | Claude subagent (M3-07) | ⬜ 待裁决：建议 ① 接受 BOSS_TIER=5 作为体型档位以外的"实体身份标记"；② 在 fish-doc/03 §1 或 §6 加一句"Boss tier 数值约定为 5（超 tier），与 Tier-4 Barracuda 区分实体身份"；③ 如希望严格按 fish-doc/00 §4.1 共享 4，则需把 `_resolve_player_boss` 改为按 `isinstance(target, Boss)` 而非 tier 区分，并修复 stats 中 `death_cause_tier=4` 与 Barracuda 致死的歧义。 |
| 17 | 2026-04-28 | M3-07 | **Boss 自管移动**（绕开 MovementSystem）。MovementSystem 的运动学（`rotate_toward(turn_rate)` + `accel/max_speed` + `desired_dir is None → drag`）是为玩家/Fish 设计；Boss 的 CHARGE 状态需要恒定 `chase_speed * BOSS_CHARGE_SPEED_MUL=1.6` 速度直冲、需要在撞墙瞬间检测并切 STUNNED、需要 ENRAGED windup 缩短等定制逻辑。M3-07 实现选择：在 `MovementSystem.step` 顶部 `if isinstance(ent, Boss): continue` 跳过 boss，由 `BossAI.step` 直接写 `boss.pos/vel/heading`，并在非 CHARGE 状态自实现简单墙体反射（与 MovementSystem 等价但更简）。 | Claude subagent (M3-07) | ⬜ 待裁决：建议 ① 接受 Boss 自管移动作为 MVP 简化（BossAI 强耦合 BOSS 状态机与运动学是合理的）；② M4 联调如发现 Boss 反射手感与 Fish 不一致，再考虑把通用反射抽到 `toy_engine.geom` 或 `fish/systems/movement.py` 的辅助函数。 |
| 18 | 2026-04-28 | M3-07 | **尾部弧 / 正面弧采用二分法（非 fish-doc/03 §3 的三分法）**。fish-doc/03 §3 写「尾后 240° 可咬」（`BOSS_TAIL_ARC_HALF_DEG=120`）+「正前 120° 致死」（`BOSS_FRONT_ARC_HALF_DEG=60`），剩余 0° 中性带（120+60+120+60=360）实际是边界对接而非中性带，描述为三分法。M3-07 实现采用二分法：`_resolve_player_boss` 分支为「尾部弧（heading+π 周围 ±120°）→ 咬」「其余（含正面 60°+ 两侧 30°）→ 致死」（player.tier==4 非 STUNNED 时）。这与 fish-doc/03 §3 的「正前 120° 致死 / 两侧 60° 中性 / 尾后 240° 咬」不完全一致，但中性带为零宽度的解读下二者等价。如解读为侧面 60° 中性（即"既不死也不咬"），则当前实现把侧面归为致死侧，玩家更难绕到尾部。 | Claude subagent (M3-07) | ⬜ 待裁决：建议 ① 若文档原意是「侧面无效」则需要在 `_resolve_player_boss` 中加 `else: pass` 不致死（但要补"持续重叠"的边界帧避免抖动）；② 若文档原意是「尾后即可咬，其余致死」则当前实现正确；③ M4 手感调优时再决定。fish-doc/03 §3 的「中性 60°」语义需要主会话明示。 |
| 19 | 2026-04-28 | M3-08 | **fish-doc/05 §3 鱼身渐变实现选型**。fish-doc/05 §3 写「填充：从 role_color 暗版（×0.6）到 role_color 亮版（×1.2）的纵向渐变」。M3-08 实现用 `GeoCanvas.gradient_ellipse(mode="linear")` 配 `palette.darken(role, 0.40)` / `palette.lighten(role, 0.40)`：darken/lighten 与文档 ×0.6/×1.2 是不同的色彩空间操作（darken 朝黑色 lerp 0.4 ≈ ×0.6 但不完全等价；lighten 朝白色 lerp 0.4 与 ×1.2 在浅色上行为相近但在暗色上偏差大）。视觉结果"暗-亮"明显，符合§3 立体感意图，但与文档严格 ×系数不一致。 | Claude subagent (M3-08) | ⬜ 待裁决：建议 ① 接受 darken/lighten 0.40 作为 MVP 实现（避免给 Palette 加 RGB 乘法；与 toy-engine/07 §4.3 暴露的 API 一致）；② 若主会话坚持严格 ×0.6/×1.2，需要在 `Palette` 上新增 `scale_rgb(name, k)` 并通知 toy-engine subagent 升级契约。 |
| 20 | 2026-04-28 | M3-08 | **Boss 「3~4 段贝塞尔轮廓」MVP 用渐变椭圆替代**。fish-doc/05 §4 写「主体：3 ~ 4 段贝塞尔轮廓的不规则椭圆（生成时一次性确定形状，避免抖动）」。`GeoCanvas.bezier_path(closed=True, fill_color=...)` 已可支持，但需要预生成"一次性确定形状"的控制点（避免每帧抖动）。M3-08 为减少代码 + 与 fish 体形一致，直接用 `gradient_ellipse(mode="linear")` 作 boss 主体；状态色（眼睛/嘴/星/边）走小圆/线段/弧。视觉上"圆椭圆"vs「不规则椭圆」少一点 boss 压迫感，但功能闭合。 | Claude subagent (M3-08) | ⬜ 待裁决：建议 ① 接受 MVP 圆椭圆 boss 进入 M4 跑分；② 若 M3-09 手感联调后觉得"boss 不够吓人"，再用 `bezier_path(segments=[(p0,p1,p2),...], closed=True, fill_color=...)` 把 boss 主体换成由 `boss.eid` seeded 的固定贝塞尔轮廓。 |
| 21 | 2026-04-28 | M3-08 | **caustics 噪声 MVP 暂未实现**。fish-doc/05 §5 Far 层写「`PALETTE_DEEP → PALETTE_MID` 径向渐变 + 噪声 caustics（用 `perlin` 生成的灰度图，蓝化叠加，alpha 60）」。M3-08 实现只画径向渐变 + 5 丛海草 + 30 气泡，**未**做 perlin caustics 层（无现成 perlin 实现 + 性能预算需要预生成 surface + LRU 缓存）。视觉效果偏「干净海底」而非「光线穿透水面」。 | Claude subagent (M3-08) | ⬜ 待裁决：建议 ① 接受 MVP 无 caustics 进入 M4 跑分；② 若主会话希望补 caustics，需要新增 `fish/render/caustics.py`（可走 `random.gauss` 为种子的灰度噪声 + `pygame.transform.smoothscale`），不需要引擎扩展。fish-doc/05 「未决问题」也已建议「预生成静态贴图 + 缓动 UV 偏移省 CPU」。 |
| 22 | 2026-04-28 | M3-09 | **慢镜时长：fish-doc/06 §3 写 0.3s，任务书写 1s**。fish-doc/06 §3 #4 写「玩家死亡瞬间 dt 缩到 0.3× 持续 0.3s（仅渲染时间，逻辑步进同步缩放）」；M3-09 任务书写「on_player_eaten → 慢镜 0.3 速 1s」。M3-09 实现按**任务书**为准（player_eaten 慢镜 1s，boss_killed 慢镜 0.4× 1.5s 文档原本未写、为本步派生）。理由：① 任务书是当前权威；② 1s 比 0.3s 更能让"被吞"瞬间被玩家感知；③ 慢镜由 `feel.get_dt_scale()` 通过 `GameLoop(logic_dt_scale=callable)` 实现，**仅 GUI 模式生效**（headless 不实例化 FeelEffects 以保持决定性，hash 与 M3-08 严格一致）。所有手感状态（trail/squash/particles/shake/slow-mo/flash/飘字）均**不**进 World snapshot/hash，由 World listener registry + render-only `FeelEffects` 持有。 | Claude subagent (M3-09) | ⬜ 待裁决：建议 ① 若主会话坚持 fish-doc/06 §3 的 0.3s，需要把 `FeelEffects._on_player_eaten` 中 `_start_slow(0.3, 1.0)` 改回 `_start_slow(0.3, 0.3)`；② 若接受任务书 1s，建议在 fish-doc/06 §3 #4 把"持续 0.3s"修订为"持续 1.0s"以保持文档一致；③ boss_killed 的 0.4×/1.5s 文档原本未给，建议在 06 §3 增补一条「反杀 Boss → 0.4× 1.5s 慢镜 + 全屏白闪」。 |
| 23 | 2026-04-28 | M3-10 | **`GameFactory` 协议签名：任务书 vs `toy_engine.tools_lib` 实际代码不一致**。M3-10 任务书原文给出的协议方法是 `create_world / create_input / is_finished / get_result`；但 `toy_engine.tools_lib.GameFactory` Protocol 实际定义是 `make_level_config(*, seed, difficulty) -> LevelConfig` / `make_world(*, level_config, seed) -> World` / `make_bot(*, name, world, rng) -> InputSource | None` / `serialize_config(level_config) -> dict` / `deserialize_config(raw) -> LevelConfig` + 可选 `bind_metrics(world, metrics)`（**EQ12 hook**：实现后 tools_lib 把 `metrics.tick(dt)` 责任交给业务）。M3-10 按用户指示「**以代码为准并登记发现**」实现 `FishGameFactory` 五方法 + `bind_metrics`。理由：① toy_engine M2-08 已经按这套签名落地并被 `tools/run_headless`/`tools/param_sweep` 验证；② `bind_metrics` 是 EQ12 闭合的关键 hook（任务书原文也提到 EQ12，但接口名错配）；③ tools_lib 的 `load_factory` 直接 `getattr(module, ATTR)` 不调用，所以 `make_factory` 必须是**实例**而非函数 → 实现为 `make_factory = FishGameFactory()` 模块级 singleton + `FISH_FACTORY` 别名兼容 `DEFAULT_FACTORY_SPEC = "fish.__main__:FISH_FACTORY"`。 | Claude subagent (M3-10) | ⬜ 待裁决（建议通过）：建议 ① 接受 toy_engine 实际签名作为正式约定；② 把 fish-doc/07 §「GameFactory 协议」一节同步为五方法 + 可选 `bind_metrics` hook；③ 在 fish-doc/00 或 07 增补一句「factory 入口必须是 `GameFactory` 实例（如 `make_factory = FishGameFactory()`），tools_lib 直接 getattr 不调用」。 |
| 24 | 2026-04-28 | M3-10 review | **`death_cause` 枚举：fish-doc/07 §6 示例只列 `Barracuda` / `Boss_charge` / `Boss_face` / `null`，但当前吃/被吃规则允许 Tier-2/3 鱼杀死低 tier 玩家**（例如 seed=0 heuristic 单局实际输出 `death_cause="Snapper"`）。GPT 审阅修复为 metrics envelope 输出具体鱼 tier 名（`Minnow/Sardine/Snapper/Barracuda`）或 `Boss_charge/Boss_face`，不再输出泛化的 `fish/boss`。 | GPT subagent (M3-10 review) | ⬜ 待裁决：建议把 fish-doc/07 §6 的 `death_cause` 枚举扩为 `Minnow/Sardine/Snapper/Barracuda/Boss_charge/Boss_face/null`；若主会话坚持只暴露 `Barracuda`，则需业务侧把非 Barracuda 鱼致死压缩到另一个枚举值或调整碰撞/生成规则。 |
| 25 | 2026-04-28 | 试玩反馈修复 | **玩家转弯过慢 → `PLAYER_TURN_RATE` 从 6.0 提至 12.0 rad/s（约 2×）**。fish-doc/06 §2 原值 6.0 rad/s 在试玩时表现为「转弯半径过大、难以对准猎物」。本次仅调整玩家常量，`FISH_TURN_RATE_RAD_S` 与 `BOSS_TURN_RATE` 不变（鱼/Boss 仍按各自档位）。改动后玩家轨迹变化 → fish 反应链变化 → snapshot_hash 基线必然改变（旧 `3b5b5beb...` → 新 `f388317ff3a436c486584ad9aa3578c2194b828b`），属预期。 | 主会话 + Claude subagent (试玩反馈) | ⬜ 待裁决：建议 ① 接受 12.0 rad/s 作为新基线；② 在 fish-doc/06 §2 把 `PLAYER_TURN_RATE` 6.0→12.0 同步回填，附"M3 试玩调参"注释；③ 若主会话希望更保守，可选 9.0 rad/s（1.5×）。 |
| 26 | 2026-04-28 | 试玩反馈修复 | **椭圆朝向 bug：斜向（45°/135°…）下椭圆长轴与 heading 垂直；正交方向（0°/90°/180°/270°）因 AABB 对称凑巧看起来正确**。`toy_engine/render/pyg.py` 的 `ellipse` / `arc` / `gradient_ellipse` 三处都用了 `pygame.transform.rotate(..., math.degrees(angle))`，但 pygame 的 transform.rotate 是「数学坐标系下 CCW」，而我们的 `heading` 是 screen-Y-down 的 CW 角 → 二者方向相反。修复：三处都改为 `-math.degrees(angle)`（坐标系换算的标准做法）。新增 `tests/test_render_pyg.py::test_ellipse_long_axis_aligns_with_heading_at_45deg` 数学采样测试（沿 heading 方向距中心 0.4·L 的像素必须在椭圆内、垂直方向必须在椭圆外），bug 修复前会失败。 | 主会话 + Claude subagent (试玩反馈) | ⬜ 待裁决：建议 ① 接受角度取负作为 GeoCanvas 的正式契约（heading=screen-Y-down 弧度）；② 在 toy-engine/07 §3 渲染模块里补一句「`ellipse / arc / gradient_ellipse` 接收的 angle 与 heading 同语义（screen-Y-down，CW 正向）；内部已为 pygame.transform.rotate 取负」；③ 决定性 hash 不受影响（渲染层不进 snapshot）。 |
| 27 | 2026-04-28 | 试玩反馈修复 | **关卡链断档：玩家无法从 Tier-0 成长到能反杀 Boss**。原因：① LevelGenerator 的 PRESSURE / BOSS 阶段 Tier-3/4 下限可能为 0（`_PRESSURE_TIER3_RANGE=(1,3)` / `_PRESSURE_TIER4_RANGE=(0,2)` / `_BOSS_TIER4_RANGE=(0,2)`），最坏采样下 Tier-3/4 几乎缺席；② LevelDirector 在 BOSS 阶段返回全 0 target → spawner 完全停刷，若玩家以 Tier-2 进入 BOSS（director 触发条件 `player.tier ≥ 2 → BOSS`）则成长链就此断裂，无法吃到 Tier-4 反杀 Boss。修复：① 把 `_PRESSURE_TIER2_RANGE/_PRESSURE_TIER3_RANGE/_PRESSURE_TIER4_RANGE` 下限提至 ≥ 1，`_BOSS_TIER3_RANGE/_BOSS_TIER4_RANGE` 下限提至 ≥ 1；② LevelGenerator 新增 **第 6 条硬约束 C6**：`_GROWTH_CHAIN_MIN[PRESSURE]={1:3,2:2,3:1,4:1}` / `_GROWTH_CHAIN_MIN[BOSS]={3:1,4:1}` 防御性校验；③ `LevelDirector.get_active_population_target()` 在 BOSS 阶段保留 Tier-3/4 的 cfg 目标值（仅抑制 Tier-1/2 的小鱼新刷），让 spawner 持续补充成长链食物；④ 同步更新 `tests/fish/test_level_generator.py::test_boss_returns_zero_target` → `test_boss_returns_zero_for_low_tiers_and_keeps_growth_chain`，`test_boss_phase_does_not_spawn_new_fish` → `test_boss_phase_suppresses_low_tier_spawn`（仅断言 Tier-1/2 无新刷）；⑤ 既有 5 条硬约束保持兼容（`PHASE_TIER4_POPULATION_MAX=3` 仍未变；C5 相邻连续性允许 0→2 的小幅跳变，新下限不触发）。bot 跑分（seed=0/difficulty=0.5/heuristic）：first_growth_time=19.38s（非空 ✓）。 | 主会话 + Claude subagent (试玩反馈) | ⬜ 待裁决：建议 ① 接受 C6 + BOSS 阶段保留 Tier-3/4 的设计；② 在 fish-doc/04 §5 把硬约束表扩到 6 条，并在 §3 的「方向性」描述里加「PRESSURE / BOSS 必须保留可吃成长链 Tier-1..4 各 ≥ 1 条」；③ 在 fish-doc/02 §4 注明「BOSS 阶段 spawner 仅抑制 Tier-1/2 新刷，Tier-3/4 仍按 cfg 补齐」；④ 玩家入 BOSS 的触发条件 `player.tier ≥ 2` 暂不动（让 boss 早进场维持紧迫感），靠 spawner 持续补 Tier-3/4 食物来保证反杀路径成立。 |

---

## 工作日志

| 时间 | 角色 | 动作 | 备注 |
|---|---|---|---|
| 2026-04-27 | 主会话 | 完成 M0 全部 9 篇文档 | 等待安排 M1 |
| 2026-04-27 | Claude subagent | 完成 M1 全部 11 篇引擎文档 + 提交 5 项契约变更 | 见上方变更登记 |
| 2026-04-27 | 主会话 | 裁决 5 项契约变更全部通过 | M0/M1 设计阶段收尾，待用户开启 M2 实现轮次 |
| 2026-04-27 | Claude subagent | 完成 M1 全部 11 篇引擎文档；Q6 决议关闭（不做 ECS，做 `GameLoop`）；登记 5 项接口变更（变更区 #1~#5）；字体选方案 A（薄 re-export）；音频暂不下沉 | 等待主会话裁决变更区 #1~#5，然后启动 M2 |
| 2026-04-27 | Claude subagent | 完成 R2 metrics 文档重写（方案 A）+ 同步 02/08 + 契约 #8 登记 | review-log 待主会话关闭 |
| 2026-04-28 | 主会话 + Claude/GPT 团队 | 完成 toy-engine M2 全部 10 步实现（M2-01..M2-10）+ POST-M2 批次 A/B 修复（详见 toy-engine progress 工作日志），引擎进入「M3 可开工」状态 | 14 个本地 commit 未推 origin |
| 2026-04-28 | 主会话 | 评估 toy-engine M2 验收通过；登记 fish M3 任务表（10 步）；标记 M3 进入「进行中」 | 准备派遣 Claude 实施 M3-01 |
| 2026-04-28 | Claude subagent (M3-01) | 实施 M3-01 项目骨架：建立 `fish/{config,entities,systems,ai,render,io}/` 包；落地 `fish/config/constants.py`（集中所有文档已写死的数值，含 Tier 阈值/Boss 状态时长/Phase 区间等，每条带文档来源注释）；落地 `fish/config/level_config.py`（`LevelConfig` 用 `frozen=True` + `default()` 工厂；`PhaseConfig`/`BossConfig` 子配置严格按 fish-doc/04 §2）；占位 `fish/main.py`（`python -m fish.main` 可跑）；新增 `tests/fish/test_skeleton.py` 5 个轻量测试。`pytest tests/fish` 5 passed；`python -m fish.main` 输出 "fish MVP — skeleton ready"。**未**实现 World/实体/系统/AI/渲染。等待主会话 commit。新增「M3 实施期发现」小节登记 2 项待裁决问题。 |
| 2026-04-28 | GPT subagent (M3-01 review) | 独立审阅 M3-01，确认未越界实现 World/实体/系统/AI/render 逻辑；小修 `fish/main.py` 的 pygame 零副作用、`LevelConfig.default()` 来源说明、`TIER_MAX` 来源注释，并补强骨架测试的类型/硬约束与 `main()` 无 pygame import 断言。 | PASS_WITH_FIXES；`pytest tests/fish -q` 5 passed；指定 import smoke 可打印 `LevelConfig.default()`；建议 Phase 枚举命名与 nested dataclass frozen 范围均裁决通过。 |
| 2026-04-28 | Claude subagent (M3-02) | 实施 M3-02 World 骨架：新增 `fish/entities/base.py`（`Entity` 基类：`eid/pos:Vec2/vel:Vec2/radius/alive`，无业务行为）；新增 `fish/world.py`（`GameResult` 枚举对齐 fish-doc/01 §4 四态 RUNNING/DEAD/VICTORY/TIMEOUT；`World` 实现 `Steppable`：`step` 仅推进 `frame_count/elapsed_s` + 缓存 `last_input_frame`/`last_effective_dt`，`snapshot()` 返回含 `player_pos/frame_count/elapsed_s/entities/game_result` 的 dict，`snapshot_hash()` 用 `_normalize_for_hash`(浮点四舍五入到 6 位小数 + Enum→name + Vec2→[x,y]) + sorted-key JSON + sha1，`is_finished()` 基于 `game_result` 或 `elapsed_s ≥ Σ phase.duration_s` 占位）；升级 `fish/main.py` 为构造 `LevelConfig.default()+SeededRng+World+GameLoop` 跑 30 帧 headless 并打印 snapshot/hash；新增 `tests/fish/test_world_skeleton.py`（5 大类 16 用例覆盖 snapshot 形状/step 推进/snapshot_hash 稳定&变更/is_finished/Steppable&HashableSteppable&GameLoop 集成）；同步修订 `tests/fish/test_skeleton.py::test_main_runs` 移除「不得 import pygame」断言（M3-02 main 已合法依赖 toy_engine.input → pygame）。`pytest tests/fish -q` **21 passed**；全仓 `pytest -q` **399 passed** 无回归；`python -m fish.main` 输出 `frames=31 elapsed_s=0.5167 ... snapshot_hash=75d7dbc8...`。**未**实现 Player/Fish/Boss/AI/render/systems。新增「M3 实施期发现」3 项待裁决（见下表）。 |
| 2026-04-28 | GPT subagent (M3-02 review) | 独立审阅 M3-02，确认 `World.step(dt, input_frame)` 与 `GameLoop._tick_once` 实参一致，headless 集成测试确由 `GameLoop` 推进；顺手补强 `_normalize_for_hash` 的 NaN/±inf 与过深嵌套处理，补测 tuple/list/嵌套 dict/None/Vec2/Enum；修正 `fish.main` 30 帧 headless 演示因浮点 cap 多跑 1 帧的问题。 | PASS_WITH_FIXES；`pytest tests/fish -q` 25 passed；全仓 `pytest -q` 403 passed；`python -m fish.main` 输出 `frames=30 elapsed_s=0.5000 ... snapshot_hash=4e2920f5...`；建议实施期发现 #3 通过、#4 修改、#5 通过并回填说明。 |
| 2026-04-28 | Claude subagent (M3-03) | 实施 M3-03 Player + MovementSystem + 输入接入：新增 `fish/entities/player.py`（`Player(Entity)` + `from_config` 把玩家放在世界中央，tier=1，运动学参数按 `PLAYER_MAX_SPEED[tier]/PLAYER_ACCEL/PLAYER_TURN_RATE` 初始化）；新增 `fish/systems/movement.py`（`MovementSystem`：`rotate_toward` 按 `turn_rate*dt` 限转向、沿 heading 加速到 `max_speed`、`desired_dir is None` 时 `vel *= exp(-PLAYER_DRAG*dt)`、`[0,WORLD_W]x[0,WORLD_H]` 边界以 `WALL_BOUNCE_DAMPING` 反射且位置 clamp）；升级 `fish/world.py`（`__init__` 创建 `self.player` + `self._movement`；`step` 中调用 MovementSystem；`snapshot()['player_pos']` 改为玩家实际位置×契约 #2 落实；`entities` 含 player dict）；升级 `fish/main.py`（`_StubInput` 每帧返回 `Vec2(1,0)` 推进 30 帧）；新增 `tests/fish/test_movement.py` 10 用例；同步修订 `tests/fish/test_world_skeleton.py::test_initial_state` 与 `tests/fish/test_skeleton.py::test_main_runs` 以反映 entities/main 输出变更。`pytest tests/fish -q` **35 passed**；全仓 `pytest -q` **413 passed** 无回归；`python -m fish.main` 输出 `frames=30 elapsed_s=0.5000 player_pos=(728.75, 360.00) heading=0.0000 snapshot_hash=e433c420...`（player_pos.x=728.75 > WORLD_W/2=640 ×）。未实现 Fish/Boss/AI/spawner/collision/render。新增「M3 实施期发现」 3 项待裁决（#6 tier 初值·#7 turn_rate/accel/drag tier 表·#8 边界反射是否取 radius）。等主会话 commit。 |
| 2026-04-28 | GPT subagent (M3-03 review) | 独立审阅 M3-03，确认 `World.snapshot()['player_pos']` 已返回玩家实际位置、`MovementSystem` 使用引擎 `rotate_toward`、`desired_dir=None` 走指数衰减、加速度与 `max_speed` clamp 符合 06 §2、`_StubInput` 结构兼容 `InputSource` 且由 `GameLoop` 调用；顺手修正玩家初始 tier 到 0，补零向量/非有限输入防护，补精确贴边向外速度立即反射，更新过期 docstring，并补强四向反射与 30 帧平行决定性测试。 | PASS_WITH_FIXES；`pytest tests/fish -q` 40 passed；全仓 `pytest -q` 418 passed（较 Claude 413 多 5 个审阅补强用例）；`python -m fish.main` 输出 `frames=30 elapsed_s=0.5000 player_pos=(724.92, 360.00) heading=0.0000 snapshot_hash=ce098c931e0b592db1fd8a58e7ebcf20dccd2af6`；未 git commit。 |
| 2026-04-28 | Claude subagent (M3-04) | 实施 M3-04 Fish + FishAI + Spawner：① `fish/config/constants.py` 新增 fish-tier 表（FISH_RADIUS/FISH_MAX_SPEED 取 02 §1，FISH_TURN_RATE_RAD_S/perception/flee/chase/WANDER 各项/SEPARATION 各项为 MVP 占位且全部带来源注释 + 「M4 调参」标记）+ SPAWNER_EDGE_MARGIN；② 新增 `fish/entities/fish.py`（`Fish(Entity)` + `Fish.spawn(eid,tier,pos,heading,rng)`，tier ∈ [1,4] 校验，每条鱼携带独立 SeededRng 子流）；③ 新增 `fish/ai/fish_ai.py`（`FishAIState` + `FishAI`：MVP 简化 FSM「player.tier > self → FLEE / < self → CHASE / 相等 → WANDER」+ 距离门 + WANDER 周期重选偏航 + 仅同 tier 的 separation 推开力，alignment/cohesion docstring 标 M4）；④ 新增 `fish/systems/spawner.py`（每 0.5s 检查 + 屏外四边随机刷新 + 朝屏内 + 走 `world.rng.spawn("spawner")` 子流，每条 fish 拿 `spawner_rng.spawn(f"fish_{eid}")` 隔离子流，TODO M3-06 接入 director 已注释）；⑤ `fish/world.py` 升级：`alloc_eid()` 公有化、`self.fishes` + `self._fish_ai` + `self._spawner`，step 顺序 spawner→fish_ai(按 eid 升序)→movement，snapshot.entities 含 `kind="fish"` + heading/tier/state，entities 输出按 eid 排序保证 hash 决定性；⑥ `fish/main.py` 改 300 帧 + Counter 打印 by tier；⑦ `tests/fish/test_fish_ai.py` 新增 17 用例（4 tier 工厂 + 半径/速度单调 + invalid tier + FLEE/CHASE/WANDER 行为 + 500 帧 WANDER 无 NaN + 60 帧并行决定性 + 600 帧 population_target 不超 + 2000 帧达成 target=8 + 屏外缘 spawn + 朝屏内 + snapshot 含 fish 字段）。`pytest tests/fish -q` **57 passed**，全仓 `pytest -q` **435 passed** 无回归；`python -m fish.main` 输出 `frames=301 elapsed_s=5.0167 ... fish_count_by_tier: tier1=8, tier2=0, tier3=0, tier4=0 (total=8) snapshot_hash=fe9ff11a...`（WARMUP target[1]=8 已拉满，target[2..4]=0 故只有 Tier 1，证明 spawner 正常工作）。未实现 collision/eat/grow/Boss/director/render。新增「M3 实施期发现」 4 项待裁决（#9 tier 编号 0..3 vs 1..4 / #10 fish 参数表占位值 / #11 FSM 简化版无滞回 / #12 §4 硬约束兜底留 M3-06）。等主会话 commit。 |
| 2026-04-28 | GPT subagent (M3-04 review) | 独立审阅 M3-04，确认 `Fish.tier` 取 1..4 与 02 §1 一致、`alloc_eid()` 公有化对 Spawner 合理、`world.rng.spawn("spawner")` + `spawner_rng.spawn(f"fish_{eid}")` 在同 seed / 同输入下逐帧决定性；顺手补强 FLEE 同位/非有限方向兜底、separation 后速度不突破 `fish.max_speed`、Spawner 生成瞬间屏外且朝内测试，并修正 `fish.main` 由 `run_headless(max_sim_seconds)` 浮点累计导致多跑 1 帧的问题，改用 `GameLoop.step_once(300)`。 | PASS_WITH_FIXES；`pytest tests/fish -q` 60 passed；全仓 `pytest -q` 438 passed；`python -m fish.main` 输出 `frames=300 elapsed_s=5.0000 ... fish_count_by_tier: tier1=8, tier2=0, tier3=0, tier4=0 (total=8) snapshot_hash=23e45bb1bc27cb25e003bdf2857450765d89e0f5`；未 git commit。 |
| 2026-04-28 | Claude subagent (M3-05) | 实施 M3-05 碰撞 + 吃/被吃 + 成长 + DEAD + 同 tier bounce：① `fish/systems/collision.py` 新增 `CollisionSystem`（优先同 tier bounce → `can_eat(player, fish)` 上金钟 → `can_eat(fish, player)` 下金钟；fish-fish 仅同 tier bounce；按 eid 升序决定性遍历）、`can_eat(a,b) = a.tier >= b.tier-1`（发现 #13）、`_elastic_bounce_same_tier`（`circle_circle_penetration` 推开 + 等质量法向速度互换 + 仅靠近时交换避免振荡）；② `fish/systems/growth.py` 新增 `GrowthSystem`（while 跨阈值 → `Player.grow_to`，上限 `TIER_MAX=4`）；③ `fish/entities/player.py` 加 `exp: float = 0.0` + `grow_to(new_tier)` 同步 radius/max_speed（clamp 到 [0, TIER_MAX]）；④ `fish/world.py` 升级：step 顺序 `spawner → fish_ai → movement → collision → growth → _cleanup_dead`；增 `on_fish_eaten / on_player_eaten / on_player_grow` hook；增 `stats: dict`（fish_eaten_count + by-tier + player_grow_count + death_cause_tier）；snapshot 新字段 `player_tier / player_exp / stats`；`game_result is not None` 后 step 仅推进计时器（DEAD 不回退）；⑤ `fish/main.py` 600 帧 + `_ChaseNearestInput` 驱动（追最近鱼，单位化 desired_dir）。新增 `tests/fish/test_collision.py` 26 用例（can_eat、1-tier 吃、严格吃、DEAD、同 tier bounce 位置+等质量法向互换、分离时不交换、fish-fish bounce、不同 tier 互不影响、多吃升级、grow_to clamp、tier 上限、一帧连升、snapshot_hash 决定性、不同 seed 分叉、DEAD 后不回退+stats 不变、CollisionSystem dt=0/已 DEAD 无作为、snapshot 新字段、stats 反映吃鱼）。同步修订 `tests/fish/test_skeleton.py::test_main_runs` 以不锁帧数（M3-04 300 → M3-05 600）。`pytest tests/fish -q` **86 passed**；全仓 `pytest -q` **464 passed**（+26 vs M3-04 基线 438）无回归；`python -m fish.main` 输出 `frames=600 elapsed_s=10.0000 player_pos=(1217.12, 454.86) heading=-0.4832 tier=0 exp=4.00 game_result=None stats={'fish_eaten_count': 2, 'fish_eaten_tier1': 2, ..., 'player_grow_count': 0, 'death_cause_tier': -1} fish_count_by_tier: tier1=8, ... (total=8) snapshot_hash=5fb8f3d0cd9ce88131d8e72237640926d63a27a3`（seed=0 + 追最近鱼输入 → 600 帧吃掉 2 条 Tier-1，exp=4<8 未跨阈，未 DEAD）。未实现 LevelDirector/Boss/render/手感。新增「M3 实施期发现」 #13（can_eat 公式裁决，主会话已预批准）。等主会话 commit。 |
| 2026-04-28 | GPT subagent (M3-05 review) | 独立审阅 M3-05，确认判定优先级等价于「同 tier bounce；player 可吃 ≤ self+1；fish 仅在 ≥ self+2 时吃 player」、`GROWTH_REWARD=(1,2,5,12,30)` 与 01 §2 一致、`GrowthSystem` while 支持跨多阈值、同 seed 决定性与 `fish/main.py` demo 输出可复现；顺手修复 `CollisionSystem` 在 DEAD 写入同一帧仍继续 fish-fish bounce 的边界问题，并为 `_elastic_bounce_same_tier` 增加非有限 penetration/velocity 防护；补 3 个测试覆盖 DEAD 同帧短路、同心 bounce 非 NaN、一帧吃多鱼跨多阈值升级。 | PASS_WITH_FIXES；`pytest tests/fish -q` 89 passed；全仓 `pytest -q` 467 passed；`python -m fish.main` 输出 `frames=600 elapsed_s=10.0000 ... fish_eaten_count': 2 ... snapshot_hash=5fb8f3d0cd9ce88131d8e72237640926d63a27a3`；未 git commit。 |
| 2026-04-28 | Claude subagent (M3-06) | 实施 M3-06 LevelGenerator + LevelDirector + spawner 接入：① 新增 `fish/systems/level_generator.py`（`LevelGenerator.generate(seed,difficulty,rng)->LevelConfig` + `validate(cfg)->[Violation]` + `LevelGenerationError`；用 `rng.spawn("level_gen").spawn(f"attempt_{k}")` 隔离重试子流，单次 attempt 内再 `attempt_rng.spawn("phases")/spawn("boss")` 与每阶段 `spawn(f"phase_{name}")` 三层派生确保同 seed 完全决定性；最多 `LEVEL_GEN_MAX_RETRIES=10` 次重试；难度 ∈ [0,1] 影响 PRESSURE Tier-4 上限 + boss.appear_time 偏置；5 大硬约束 C1=每阶段至少有可吃猎物 / C2=tier4 ≤ 3 / C3=WARMUP 仅 Tier-1..2 且 Tier-1 ≥ 1 / C4=boss.appear_time ∈ [25,60]s / C5=相邻阶段每档 max(a,b) ≤ 3·max(min(a,b),1)；BOSS/REVENGE PhaseConfig.duration_s 填新增 `BOSS_PHASE_TIMEOUT_S=60.0` / `REVENGE_PHASE_TIMEOUT_S=30.0` 作事件驱动兜底；spawn_rate = `max(0.05, n/8.0)` 占位）；② 新增 `fish/systems/level_director.py`（`LevelDirector` 维护 `current_phase / phase_elapsed_s / _boss_was_present / _transition_log`；step 优先判 `elapsed_s ≥ TIMEOUT_S=180` → `GameResult.TIMEOUT`，再算迁移：WARMUP→PRESSURE 按 duration；PRESSURE→BOSS 按 duration ∨ `player.tier ≥ 2`；BOSS→REVENGE 按 `player.tier ≥ TIER_GIANT=4` ∨ (`boss_was_present ∧ boss is None`)；REVENGE 完成 → VICTORY；切换时把 `world._spawner._time_since_last_check=∞` 触发立即重新刷；BOSS 阶段 `get_active_population_target()` 返回全 0 抑制普通鱼）；③ `fish/systems/spawner.py` 改读 `world.director.get_active_population_target()`，无 director 时回退到 WARMUP target；④ `fish/world.py` __init__ spawner 之后构造 director（spawner getattr-fallback 兼容），step 顺序 director→spawner→fish_ai→movement→collision→growth→cleanup，snapshot 增 `phase`/`phase_elapsed_s`，**`is_finished()` 简化为 `return self.game_result is not None`（裁决 #4 已在本步落实）**；⑤ `fish/main.py` 重写为 `LevelGenerator.generate(seed=0, difficulty=0.5, rng=SeededRng(0))` + 60s headless（3600 帧）+ 改进版 `_ChaseNearestInput`（识别 prey/threat，威胁 < 200px 时径向逃离 + 0.6 朝中心偏置避免被推到墙角，使玩家在 60s 内升到 tier=2 触发 PRESSURE→BOSS）；⑥ 新增 `tests/fish/test_level_generator.py` 共 68 用例（TestLevelGeneratorDeterminism 4 + TestLevelGeneratorConstraints 参数化 seed 0..39 + 5 + TestLevelGeneratorRetries monkeypatch validate + TestLevelDirectorTransitions 7（直接调 director.step 绕开 collision 死法）+ TestGetActivePopulationTarget 5 + TestSpawnerSuppressedInBoss + TestIsFinishedRulingFour（裁决 #4 验证）+ TestHeadlessDeterminism 30s 两次 hash 序列一致 + 不同 seed 分叉）；⑦ 同步修订 `tests/fish/test_world_skeleton.py` 把「按 elapsed_s 终止」两条旧测试改名为「不按 elapsed_s 终止」+「按 game_result 终止」、`tests/fish/test_fish_ai.py::test_eventually_reaches_warmup_target` 限制循环到 8s 内（避免 director 推进到 PRESSURE 改变 target）。`pytest tests/fish -q` **157 passed**（+68 vs M3-05 基线 89）；全仓 `pytest -q` **535 passed**（+68 vs M3-05 基线 467）无回归；`python -m fish.main` 输出 `level: seed=0 difficulty=0.50 warmup=12.28s pressure=23.72s boss_appear=42.16s` + `phase_transitions (2): t=12.27s WARMUP→PRESSURE; t=36.00s PRESSURE→BOSS`，`frames=3601 elapsed_s=60.0167 player_pos=(886.60, 569.84) heading=2.1934 tier=2 exp=28.00 phase=BOSS phase_elapsed_s=24.00 game_result=None stats={'fish_eaten_count': 8, 'fish_eaten_tier1': 4, 'fish_eaten_tier2': 4, 'player_grow_count': 2} snapshot_hash=40481f45d643667933dfe17b8f892631ccc44293`（DoD「至少 2 阶段切换」达成）。**未**实现 Boss 实体（M3-07）/ render（M3-08）/ 手感（M3-09）/ tools 联调（M3-10）。新增「M3 实施期发现」 #14（生成器抽样区间 / 校验阈值 / spawn_rate 公式来源）+ #15（裁决 #4 落实记录）。等主会话 commit。 |
| 2026-04-28 | GPT subagent (M3-06 review) | 独立审阅 M3-06，确认 `LevelGenerator` 使用命名子流隔离 attempt/phase/boss、`World.is_finished()` 仅依赖 `game_result`、snapshot 中 `phase` 已用 `.name` 字符串稳定哈希、BOSS 阶段普通鱼刷新抑制且 REVENGE 可恢复；顺手修复 `validate()` 对 C1 的弱化（改为按阶段可吃 tier 上限检查，WARMUP Tier-1 兜底保留）、补 NaN/inf/负数校验与错误上下文、补 C5 公式上下文、修正 LevelDirector 在玩家未达 tier2 时不能早于 `boss.appear_time_s` 进入 BOSS（同时保留 PRESSURE duration 节奏下限），并补 8 个回归测试覆盖重试递增、非有限值、C1/C5、Boss 触发下限、REVENGE 恢复刷新与 snapshot phase/hash。 | PASS_WITH_FIXES；`pytest tests/fish -q` 165 passed；全仓 `pytest -q` 543 passed；`python -m fish.main` 输出 2 次阶段切换（12.28s WARMUP→PRESSURE，42.17s PRESSURE→BOSS），60s 仍在 BOSS 且 `game_result=None` 合理；未 git commit。 |
| 2026-04-28 | Claude subagent (M3-07) | 实施 M3-07 Boss 实体 + BossAI 6 态机（PATROL/CHASE/CHARGE_WINDUP/CHARGE/STUNNED/ENRAGED）+ 玩家复仇判定 + Tier-4 提示：① `fish/entities/boss.py` 新增 `Boss(Entity)` + `Boss.spawn(eid, world_size, rng, *, player_pos, cfg_boss)`（按 player_pos 选最远屏外边、tiebreak 走 rng；tier=BOSS_TIER=5 标记「超 tier」实体身份，与 Tier-4 Barracuda 区分，发现 #16）；② `fish/ai/boss_ai.py` 新增 `BossState` + `BossAI.step`（None→PATROL 首帧初始化；hp/max_hp<0.30→`enraged=True` 同时把 CHASE 标签改为 ENRAGED；按状态分发；CHARGE 撞 [0,W]×[0,H] 即转 STUNNED 并 clamp + 启动冷却；ENRAGED 下 windup×0.5、cooldown×0.6、sense×1.30；Boss 不参与 MovementSystem，由 BossAI 自处理位置/速度/heading + 非 CHARGE 墙体反射，发现 #17）；③ `fish/entities/player.py` 加 `invuln_remaining: float`（咬中 boss 后 0.5s 不被致死）；④ `fish/systems/movement.py` 跳过 Boss；⑤ `fish/systems/collision.py` 加 `_resolve_player_boss`（intro 期跳过；player.tier<4→DEAD（写 death_cause_tier=5）；player.tier==4 + 尾部 240° → 咬一口；player.tier==4 + STUNNED 任意角度 → 咬；其余 player.tier==4 接触 → DEAD；invuln 期内跳过致死分支，发现 #18）+ `_bite_boss`（hp--、bite_count++、置 STUNNED、player.invuln=0.5、hp<=0→boss.alive=False+world.boss=None）+ 每帧自减 invuln + 计算 `world.tier4_warning`；⑥ `fish/world.py` 加 `boss/tier4_warning/_boss_ai/spawn_boss/_cleanup_dead boss/_boss_snapshot` + 3 个 hook；step 顺序在 fish_ai 后 → BossAI → movement；snapshot 增 player_invuln_remaining/tier4_warning/boss；⑦ `fish/systems/level_director.py` 在 `_transition_to(Phase.BOSS)` 调 `world.spawn_boss()`，并把 `_boss_was_present` 标记下沉到 `world.boss is not None` 即置 True（保证 BOSS→REVENGE 判定可工作）；⑧ `fish/main.py` 改 90s（5400 帧）+ `_ChaseNearestInput` 升级（boss 出现且 intro 完成 + player.tier>=4 时追 boss 尾点；player.tier<4 时把 boss 当威胁；threat-flee 阈值 200→250px）+ 改用 `loop.step_once(1)` 抓 boss state 切换日志；⑨ `fish/config/constants.py` 新增 BOSS_TIER=5 / BOSS_RADIUS=90.0 / BOSS_BITE_DAMAGE=1 / BOSS_PATROL_SPEED_RATIO=0.40 / BOSS_PATROL_TURN_INTERVAL_S=2.5 / BOSS_ENRAGED_WINDUP_MUL=0.50（全部带「fish-doc/03 §6 + MVP 占位」注释）；⑩ `tests/fish/test_boss.py` 新增 27 用例（TestBossSpawn 3 + TestBossAI 10 + TestPlayerBossCollision 7 + TestTier4Warning 3 + TestDirectorBossPhase 3 + TestDeterminism 1）。`pytest tests/fish -q` **192 passed**（+27 vs M3-06 基线 165）；全仓 `pytest -q` **570 passed**（+27 vs M3-06 基线 543）无回归；`python -m fish.main` 输出 `level: seed=0 difficulty=0.50 warmup=12.28s pressure=23.72s boss_appear=42.16s` + `phase_transitions (2): t=12.28s WARMUP→PRESSURE; t=42.17s PRESSURE→BOSS` + `boss_state_transitions (11): <none>→PATROL@42.17s, PATROL→CHASE@50.17s, CHASE→CHARGE_WINDUP@51.08s, →CHARGE@51.88s, →CHASE@53.40s, →CHARGE_WINDUP@63.08s, →CHARGE@63.88s, →STUNNED@65.02s, →CHASE@67.03s, →CHARGE_WINDUP@74.02s, →CHARGE@74.82s` + `frames=4531 elapsed_s=75.5167 player_tier=1 game_result=DEAD tier4_warning=True boss state=CHARGE hp=3/3 bites=0`（player tier=1 在 BOSS 阶段被 boss 撞死，符合预期；DoD「至少 boss spawn + 一次状态切换日志」达成且观察到完整 PATROL→CHASE→CHARGE_WINDUP→CHARGE→STUNNED 闭环）。**未**实现 render（M3-08）/ 手感粒子（M3-09）/ bot/metrics/tools（M3-10）。新增「M3 实施期发现」 #16（Boss tier=5 vs 共享 4）+ #17（Boss 自管移动 vs MovementSystem 统一）+ #18（尾部弧/正面弧二分法 vs fish-doc 三分法 240°/60° 之外的 60° 中性侧带）。等主会话 commit + GPT review。 |
| 2026-04-28 | GPT subagent (M3-07 review) | 独立审阅 M3-07，确认 `BossState` 应为 5 个核心态且 `boss.enraged` 为修饰位；顺手修复/补强：PATROL 强制 8s 后才按 `sense_radius` 追击、`player.invuln_remaining` 同时节流连续 boss bite、同心/非有限角度按危险接触、Tier-4 warning 仅统计屏内 Tier-4 fish、Boss 死亡后先切 REVENGE 再等窗口结束写 VICTORY；同步修订 M3-07 progress 行与 `tests/fish/test_boss.py`。 | PASS_WITH_FIXES；`pytest tests/fish -q` **198 passed**；全仓 `pytest -q` **576 passed**；`python -m fish.main` 输出 2 次阶段切换 + 9 次 boss 状态切换，最终 `game_result=DEAD`、`death_cause_tier=5`、`snapshot_hash=3b5b5beb82db79074906767d87adb60a6fb28818`；未 git commit。 |
| 2026-04-28 | Claude subagent (M3-08) | 实施 M3-08 视觉层（Palette + draw_fish/draw_boss + 视差背景 + UI + 字体 + GUI 入口）：① `fish/render/palette.py`：基于 `toy_engine.render.Palette` 构造 `FISH_PALETTE`，锁定 fish-doc/05 §1 五个背景色（`bg_deep/mid/shallow/foam/highlight`）+ 五个角色色（`role_player/prey/peer/threat/boss`）+ UI 附加色（`ui_text/warning/bar_bg/bar_fill/bar_boss/dim`）+ Boss 状态眼色（`boss_eye_patrol/chase/charge/stunned`）；`tier_to_role_name(fish_tier, player_tier)` 按裁决 #13 「fish.tier ≤ player.tier → prey；==player.tier+1 → peer；其余 → threat」映射。② `fish/render/visuals.py`：纯函数式绘制（**不直接 import pygame**，全部走 `GeoCanvas` 抽象，由 TestModuleImports 守护）：`draw_player/draw_fish/draw_boss`（卡通鱼 = 椭圆体 4×R 长 + 三角尾 sin(t·6+phase)·18° 摆动 + 上下鳍 alpha=120 + 白眼+黑瞳 + 高光弧 alpha=80；boss 主体 `gradient_ellipse` 2.4R×1.6R + intro 渐显环 + ENRAGED 红外环 + 双眼按状态变色（patrol=黄/chase=橙/charge=红/stunned=灰）+ CHARGE_WINDUP 红线警告 + 嘴 1.5s 张合 0.4s 露白齿 + STUNNED 头顶 3 旋转星）、`draw_parallax_background`（径向 deep→mid 渐变 + 5 丛贝塞尔海草按 t 摆动 + 30 气泡循环上浮，按 player_offset×{0.1,0.4,1.0} 三层视差，clamp ±30px per fish-doc/05 §5 末段）、`draw_ui`（左上 Tier 徽章 + EXP 进度条；右上 elapsed/phase；顶部 Tier-4 警示；右下 BOSS HP + ENRAGED 标；font=None silent skip）、`draw_game_over`（DEAD=红/VICTORY=金/TIMEOUT=白 + ui_dim alpha=160 半透遮罩 + "Press ESC to quit"；game_result=None 直接 return 不动像素）。③ `fish/render/pyg_renderer.py`：`PygRenderer(canvas, palette, font)` 含 `__slots__`，`render(world)` 顺序 background → fishes(按 eid 排序，alive only) → player → boss(if alive) → ui → game_over(if game_result is not None)；`player_offset` = `player.pos - (W/2, H/2)`。④ `fish/render/__init__.py` re-export `FISH_PALETTE/build_fish_palette/tier_to_role_name/PygRenderer`。⑤ `fish/main.py` 升级：保留 `run_headless(seed,difficulty,max_frames,verbose)→World`（M3-04..07 兼容入口）+ 新增 `run_gui(seed,difficulty,title,end_screen_seconds=3.0)`（懒导入 pygame/font/KeyboardMouseInput/GeoCanvas/PygRenderer；`pygame.init()` + `GeoCanvas.create_window(1280,720,title)` + `load_font(20,"consolas","microsoftyahei",fallback_size=20)` 走 toy_engine.font 走 font_utils；`KeyboardMouseInput(viewport=(1280,720))`；自管 `pygame.event.get()` + ESC/QUIT；`loop.step_once(1)` 单步若未终态，否则保持终态画面 `end_screen_seconds=3` 后 break；renderer.render+canvas.present 每帧；`pygame.font.quit()/pygame.quit()` 收尾）；`main()` 默认走 GUI，`python -m fish.main --headless` 旁路。⑥ 字体路径与契约：`toy_engine.font.load_font(20, "consolas", "microsoftyahei", fallback_size=20)` per task spec。⑦ `tests/fish/test_render.py` 共 29 用例（TestPalette 4 + TestBackground 3 + TestDrawEntities 7 + TestUI 6 + TestPygRenderer 7 + TestModuleImports 2）：覆盖 5 个角色色全 tier、boss 5 状态 × {enraged True/False} 笛卡尔、player/fish/boss=None 边界不崩、UI font=None silent skip、PygRenderer 跑 60 帧 stepped world 不崩、终态遮罩三态都画、game_over game_result=None 不动像素、`canvas.present()` 离屏 no-op、`pygame.font.Font(None, size)` 替代有 bug 的 `SysFont(None,...)`、visuals/pyg_renderer 模块顶层 `__dict__` 不含 'pygame' 键。⑧ `tests/fish/test_skeleton.py::test_main_runs` 同步修订为 monkeypatch `sys.argv=["fish.main", "--headless"]`（main 默认入口已改 GUI）。`pytest tests/fish -q` **227 passed**（+29 vs M3-07 基线 198）；全仓 `pytest -q` **605 passed**（+29 vs M3-07 基线 576）无回归；`python -c "from fish.render import FISH_PALETTE, PygRenderer; from fish.render import visuals; from toy_engine.render import GeoCanvas; ..."` import smoke OK，`SDL_VIDEODRIVER=dummy` 下不开窗口。**未**实现 手感粒子（M3-09）/ bot/metrics/tools（M3-10）。新增「M3 实施期发现」 #19（rgb 渐变实现选型 darken/lighten 0.40 vs fish-doc/05 §3 "×0.6/×1.2"）+ #20（boss 主体 `gradient_ellipse` 简化 vs fish-doc/05 §4 「3~4 段贝塞尔轮廓」）+ #21（caustics noise 层 MVP 暂未实现，仅径向渐变占位）。等主会话 commit + GPT review。 |
| 2026-04-28 | GPT subagent (M3-08 review) | 独立审阅 M3-08，确认 palette 锁定色与 fish-doc/05 §1 对齐、渲染函数走 `GeoCanvas`、测试使用 dummy/offscreen、`main()` 默认 GUI 且 `--headless` 旁路可测；顺手修复 `tier_to_role_name()` 对裁决 #13 的推导（同 tier 优先 bounce 应为 `role_peer`，可吃一档为 `role_prey`，player.tier=4 时普通鱼无 `role_threat`）以及 `draw_parallax_background()` 远层未实际应用 0.1× 视差偏移的问题，并在既有 render 测试中补对应断言。 | PASS_WITH_FIXES；`pytest tests/fish -q` **227 passed**；全仓 `pytest -q` **605 passed**；`python -m fish.main --headless` 输出 2 次阶段切换 + 9 次 boss 状态切换，最终 `frames=4315 elapsed_s=71.9167 game_result=DEAD snapshot_hash=3b5b5beb82db79074906767d87adb60a6fb28818`；未 git commit。 |
| 2026-04-28 | Claude subagent (M3-09) | 实施 M3-09 手感（trail / squash / 三件套粒子 / 屏震 / 慢镜 / 渐隐 + flash overlay）：① 新增 `fish/render/feel.py`（`TrailRenderer`：deque maxlen=12 + 0.05s 采样间隔 + render 时按位置序列 alpha40→220 / width 2→5 渐变线段；`SquashRenderer.compute(speed,max_speed)`：speed=0→(1,1)，speed≥max→(1.15,0.9)，线性插值且 over-speed clamp、max_speed≤0 安全返回 (1,1)；`FeelEventBus`：append-only 队列 + drain；`FeelEffects`：组合 `ParticleSystem(capacity=256)` + 委托给 `GeoCanvas.shake` 的屏震 + 慢镜计时（剩余>0 时 `get_dt_scale()` 返回 scale，到期回 1.0）+ flash overlay（`get_flash_overlay()` 返回 (rgb, alpha) 线性衰减）+ 飘字 list（30 px/s 上飘 + alpha 衰减）+ 5 个事件处理器：`fish_eaten` 8 颗目标主色碎屑 + 4 颗气泡 + "+N" 飘字 + 1px×0.1s 微震；`player_eaten` 16 颗红色大爆炸 + 8px×0.5s 大震 + 0.3 速 1s 慢镜（任务书；fish-doc/06 §3 写 0.3s，发现 #22）+ 红色 0.4s flash；`boss_killed` 32 颗金 + 16 颗深紫 + 10px×0.6s 大震 + 0.4 速 1.5s 慢镜 + 白色 0.5s flash；`boss_bitten` 12 颗红 + 4px×0.25s 中震；`player_grow` 14 颗金 + "TIER UP!" 飘字 + 2px×0.2s 小震）。② `fish/world.py` 升级：加 `self._listeners: list[Callable]` + `register_listener(fn)` + `_emit(event)`（捕获 listener 异常静默吞掉以保护 World 推进）；5 个 hook（`on_fish_eaten/on_player_eaten/on_player_eaten_by_boss/on_boss_bitten/on_boss_killed/on_player_grow`）末尾追加 `self._emit({type, ...pos, ...tier})` 事件分发；**所有手感状态零侵入 snapshot/hash**。③ `fish/render/visuals.py::draw_player(squash=(1,1))` + `_draw_cartoon_fish(squash=...)` 把 length×=sx、width×=sy（sx/sy<=0 退回 1.0）；默认 (1,1) 严格向后兼容现有 27 个 render 测试。④ `fish/render/pyg_renderer.py::render(world, *, feel=None)` 新增可选 feel 参数：在 fishes/player/boss 之后、UI 之前 `feel.render(canvas, palette, font)`，在 game_over 之后 `feel.render_flash_overlay(canvas)`（with_no_shake 全屏半透）；player 绘制时 `feel.squash.compute(hypot(vel.x,vel.y), max_speed)` 算 squash 传 visuals。⑤ `fish/main.py::run_gui` 升级：实例化 `FeelEffects(FISH_PALETTE, rng=SeededRng(seed).spawn("feel"))` + `feel.attach_canvas(canvas)` + `world.register_listener(feel.handle)`；`GameLoop(logic_dt_scale=lambda _s: feel.get_dt_scale())` 用 callable 替代不存在的 `set_dt_scale` API（同等机制）；每帧 `feel.step(DT, player_pos=world.player.pos)` 用基准 dt（**不**乘慢镜 scale，否则永远不结束）；renderer.render 传 feel；headless 路径**不**实例化 FeelEffects 以保持决定性。⑥ 新增 `tests/fish/test_feel.py` 17 用例（TestTrailRenderer 3 + TestSquashRenderer 4 + TestFeelEventBus 2 + TestFeelEffects 5 + TestDeterminismUnchanged 3：`test_hash_sequence_identical_with_or_without_listener` 同 seed 跑 60 帧，注册/不注册 listener 的 `snapshot_hash` 序列**严格相等**，且 60 个 hash 不同值证明状态在变；`test_listener_receives_events_when_collision_happens` 触发 fish_eaten；`test_listener_exception_does_not_break_world` 抛异常 listener 不影响 World 推进）。`pytest tests/fish -q` **244 passed**（+17 vs M3-08 基线 227）；全仓 `pytest -q` **622 passed**（+17 vs M3-08 基线 605）无回归；`python -m fish.main --headless` 仍输出 `frames=4315 elapsed_s=71.9167 game_result=DEAD snapshot_hash=3b5b5beb82db79074906767d87adb60a6fb28818`（与 M3-08 严格一致 ✓ 决定性未变）。**未**实现 bot/metrics/tools 联调（M3-10）。新增「M3 实施期发现」 #22（慢镜时长 fish-doc/06 §3 0.3s vs 任务书 1s 差异）。等主会话 commit + GPT review。 |
| 2026-04-28 | Claude subagent (M3-10) | 实施 M3-10 BotInput + MetricsCollector 绑定（闭合 EQ12）+ GameFactory + tools 联调：① 新增 `fish/ai/bot_player.py`（`BotInput(BotInputBase)` 启发式：威胁逃避（≥ptier+2 within 200px）→ tier-4 反杀 boss（STUNNED 直追 / 尾部 240° 内追尾点 / 否则斜插尾点）→ tier<4 远离 boss（<320px）→ 追猎物（≤ptier+1 最近）→ 漂移；零向量 / 非有限走 self.rng 随机角度）。② 新增 `fish/io/metrics_adapter.py`（`FishMetricsListener`：events→`record_event`；`on_frame_end(dt)` 接管 `metrics.tick(dt)`、维护 starvation_dt 累加 / near_miss rising-edge / boss_appear_t / player_max_tier / player_death_t；终态走 `_finalize` 写 5 大原料 + 6 顶层字段 + 调 `metrics.finish`；fallback 路径走 `write_envelope_before_finish` 仅写指标不调 finish；二者 `_finalized` 标志互斥幂等）。③ 新增 `fish/factory.py`（`FishGameFactory` 实现 `GameFactory` 五方法 `make_level_config` / `make_world` / `make_bot` / `serialize_config` / `deserialize_config` —— **签名以 `toy_engine.tools_lib` 代码为准，与任务书原文 `create_world / create_input / is_finished / get_result` 不一致，发现 #23**；`bind_metrics(world, metrics)` 闭合 EQ12：`world.register_listener(listener.handle)` + wrap `world.step` 末尾追加 `listener.on_frame_end(dt)` 接管 tick + wrap `metrics.finish` 在 fallback 路径前先写指标；模块级 `make_factory = FishGameFactory()` singleton 兼容 `--factory fish:make_factory`，`FISH_FACTORY` 别名兼容 `DEFAULT_FACTORY_SPEC = 'fish.__main__:FISH_FACTORY'`）。④ 新增 `fish/__main__.py` re-export FISH_FACTORY / make_factory；更新 `fish/__init__.py` re-export。⑤ `fish/main.py::run_gui` 新增 `--bot` 旁路用 BotInput 替代 KeyboardMouseInput（GUI 看 bot 表演）。⑥ 新增 `tests/fish/test_bot.py` 11 用例（BotInputBase 子类 / 漂移 / 威胁逃避 / 追猎物 / tier+1 视为 prey / 远离 boss / STUNNED 反杀 / 同 seed 决定性 / 30s 跑不抛）。⑦ 新增 `tests/fish/test_metrics_adapter.py` 11 用例（顶层 6 字段 + 5 metrics 段 + engine_extras + result ∈ {DEAD/VICTORY/TIMEOUT} + fail_rate=null + JSON 可序列化 + starvation_ratio ∈ [0,1] + near_miss_count 非负 int + duration_s 一致 + duration_frames>0 证明 EQ12 接管 tick）。⑧ 新增 `tests/fish/test_tools_integration.py` 2 子进程用例（`python -m tools.run_headless --factory fish:make_factory --seed 0 --difficulty 0.5 --max-sim-seconds 20 --out tmp.json` + `python -m tools.param_sweep --factory fish:make_factory --difficulty 0.5 --seeds 5 --max-sim-seconds 20 --out tmp.csv`，标 @pytest.mark.slow，pytest.ini 注册 `slow` marker）。`pytest tests/fish -q` **266 passed**（+22 vs M3-09 基线 244）；全仓 `pytest -q` **644 passed**（+22 vs M3-09 基线 622）无回归。Tools 联调（180s 默认 max-sim、--bot heuristic）：`run_headless` envelope 顶层 6 字段 + metrics 5 项全有；`param_sweep` 输出 1 行（1 difficulty × 5 seeds）。**EQ12 状态：✅ 闭合 —— bind_metrics hook 通过 wrap step + finish 双道接管 `metrics.tick` / 终态写入；`duration_frames > 0` 与 `metrics.<5项>` 全部出现验证 hook 实际生效**。新增「M3 实施期发现」 #23。**M3 全部 10 步完成**。等主会话 commit + GPT review。 |
| 2026-04-28 | GPT subagent (M3-10 review) | 独立审阅 M3-10 并真实运行验收命令；顺手修复 3 个中低风险问题：① metrics adapter 记录 `entered_boss` / `counter_kill` 事件，修正 `param_sweep` 的 `entered_boss_rate` / `counter_kill_rate` 数据源；② `death_cause` 从泛化 `fish/boss` 改为具体鱼 tier 名或 `Boss_charge/Boss_face`，并登记发现 #24；③ `bind_metrics` 注入的 `on_frame_end` / fallback finalize hook 增加 warning 隔离，避免 metrics listener 异常导致 World/finish 崩溃；补 6 个回归测试。 | PASS_WITH_FIXES；修复后 `pytest tests/fish -q` **272 passed**，全仓 `pytest -q` **650 passed**；指定 tools 命令均跑通并清理 `_r.json/_s.csv`；两次 `python -m fish.main --headless` 均保持 `snapshot_hash=3b5b5beb82db79074906767d87adb60a6fb28818`；未 git commit。 |
| 2026-04-28 | Claude subagent (M3 试玩反馈) | 实施 3 项修复（用户实际玩 `python -m fish.main` 后反馈）：① **#25 玩家转弯过慢**：`fish/config/constants.py::PLAYER_TURN_RATE` 6.0→12.0 rad/s（约 2×），仅影响玩家、不影响 fish/boss；② **#26 椭圆朝向 bug**：`toy_engine/render/pyg.py` 三处 `pygame.transform.rotate(..., math.degrees(angle))` 全部改为 `-math.degrees(angle)`（pygame transform.rotate 是数学坐标系 CCW，而我们的 heading 是 screen-Y-down CW，符号相反；正交方向因 AABB 对称凑巧看起来正确，斜向 45°/135°/… 时长短轴互换 90°），新增 `test_render_pyg.py::test_ellipse_long_axis_aligns_with_heading_at_45deg` 数学采样测试；③ **#27 关卡链断档**：`fish/systems/level_generator.py` 把 PRESSURE/BOSS 阶段的 Tier-3/4 下限提到 ≥ 1，新增第 6 条硬约束 `C6 _GROWTH_CHAIN_MIN`；`fish/systems/level_director.py::get_active_population_target` 在 BOSS 阶段保留 Tier-3/4 cfg 目标值（仅抑制 Tier-1/2 新刷），让 spawner 持续补成长链食物；同步更新 2 个 director 测试名称与断言。`pytest tests/fish -q` **272 passed**（与 M3-10 review 一致）；全仓 `pytest -q` **651 passed**（+1: 新增 ellipse 数学测试）；两次 `python -m fish.main --headless` 均输出 `snapshot_hash=f388317ff3a436c486584ad9aa3578c2194b828b`（决定性 ✓；新基线，旧 `3b5b5beb...` 因 PLAYER_TURN_RATE 改变而失效，属预期）；`python -m tools.run_headless --factory fish:make_factory --seed 0 --difficulty 0.5 --bot heuristic --out _r.json`：`first_growth_time=19.38s（非空 ✓）`、`player_max_tier=1`、`result=DEAD`、`death_cause=Barracuda`、`duration=26.43s`。新增「M3 实施期发现」 #25/#26/#27。未 git commit。 |
