"""tools/ — toy-engine MVP CLI scripts (run_headless / param_sweep / replay).

These are scripts, not part of the ``toy_engine`` package; this ``__init__.py``
exists only so they can also be imported (``from tools.run_headless import main``)
during testing without going through subprocess.
"""
